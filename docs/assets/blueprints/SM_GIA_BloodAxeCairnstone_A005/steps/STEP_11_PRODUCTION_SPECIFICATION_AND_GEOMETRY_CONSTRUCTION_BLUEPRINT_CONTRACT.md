# A005 Step 11 Production Specification And Geometry Construction Blueprint Contract

Status: approved for execution and completion by current Flamestrike authority

Artifact classification: `authoritative execution contract`

Contract ID: `A005-CR-STEP11-PROD-SPEC-GCB-A01`

Date: 2026-07-20

## Flamestrike Authority

After the mandatory post-Step-10 restart, Flamestrike stated:

> resume, I am giving you full authority to do whatever you need to to
> complete step 11 from start to finish, You have full approval and Authority

This statement authorizes preparation, execution, validation, visible review,
approval closeout, checkpointing, and scoped Git closeout of this exact Step 11
contract. It does not override Core, the approved fresh-start firewall, the
Step 11 planning-only boundary, or the mandatory restart after completion.

## Controlling Decision

Approve one buildable production specification and geometry construction
blueprint that maps every planned visible contour, component, interface,
source-invisible closure, bottom, pivot/origin, ownership sector, topology
group, and technical overlap to exact A005 evidence, an approved Step 10 rule,
or a clearly labeled Step 11 technical construction rule that does not broaden
source authority.

## Core Pre-Action Check

1. Governing principle: Evidence-Bound Decision Rule under First Principles.
2. Blueprint authority: approved fresh-start plan Step 11 and AetherForge
   source-ownership/geometry-construction rules.
3. Flamestrike authority: the exact statement above grants Step 11 start-to-
   finish authority after the completed mandatory restart.
4. Smallest sufficient change: planning records, read-only validation support,
   one visible planning review packet, status/index closeout, and no production
   artifact.
5. Reviewable decision: `pass`, `rejected`, or `blocked` Step 11 construction
   blueprint.
6. Drift risk: source/interpretation collapse, legacy A001-A004 contamination,
   physical cross-view pairing, hidden geometry disguised as evidence, or
   premature DCC/Unreal escalation. Controls are the immutable input lock,
   vertex-authority coverage map, explicit technical-rule registry, output
   allowlist, production-root absence checks, and fail-closed audit.

## Locked Controlling Inputs

The immutable hashes and exact pre-action state are recorded in:

`manifests/STEP_11_INPUT_LOCK.json`

The controlling asset-specific chain is:

- approved A005 charter, source lock, and fresh-start firewall;
- approved Step 09 exact-data index, correspondence, disagreement/unknown,
  and pre-geometry audit;
- selected production targets `220 / 120 / 90 / 140 / 110 / 35 cm` with no
  claim that old per-panel calibration is one physical transform;
- approved N3 C-004 outer boundary and K80 CL-003 target boundary;
- approved sixteen CL-003 target mappings;
- approved R3, R5, R7, and R9 symbolic ownership/coupling chain;
- authoritative final ten-item Step 10 disposition registry, ownership map,
  remaining-block record, output, and Step 11 handoff.

## Required Step 11 Outputs

This execution must create exactly the following new planning/support outputs:

- this contract;
- `manifests/STEP_11_INPUT_LOCK.json`;
- `manifests/STEP_11_TECHNICAL_RULE_REGISTRY.json`;
- `manifests/STEP_11_CONSTRUCTION_BLUEPRINT.json`;
- `manifests/STEP_11_VERTEX_AUTHORITY_MAP.json`;
- `manifests/STEP_11_VALIDATION_MANIFEST.json`;
- `review/STEP_11_PRODUCTION_BLUEPRINT_REVIEW.md`;
- `steps/STEP_11_OUTPUT_RECORD.md`;
- `handoffs/STEP_11_TO_STEP_12_HANDOFF.md`;
- `Tools/DCC/audit_bloodaxe_cairnstone_a005_step11_blueprint.py`.

The mutable A005 approval log, artifact index, and reset/resume record may be
updated only with the completed Step 11 result. The project recovery journal
may be appended only by the required checkpoint utility and is excluded from
the scoped commit because it already contains unrelated unstaged history.

## Required Blueprint Content

The construction blueprint must include:

- asset name and Unreal `Static Mesh` role;
- centimeter scale, axes, construction frame, origin, and pivot;
- ordered construction for C-004, C-003, C-002, C-001, then source-visible
  decorative consumers C-005/C-006/C-007;
- per-feature measurement/evidence links;
- source-versus-interpretation classification for every planned vertex group;
- separate watertight topology ownership for C-001 through C-004;
- hidden closure and positive-overlap rules for CL-001 through CL-003;
- source-invisible Z=0 bottom rule;
- combined-versus-modular runtime decision;
- planning-level UV, material-slot, texture-name, and map strategy;
- LOD0 triangle target and LOD1-LOD3 targets;
- collision plan;
- DCC, export, texture, Unreal, and review paths;
- numeric, topology, source-projection, provenance, and firewall validators;
- source-aligned front/back/left/right/top/perspective review cameras;
- blocked methods and Step 12 fail-closed conditions.

## Step 11 Technical-Rule Boundary

Step 11 may register only the minimum construction conventions needed to make
the approved Step 10 rules buildable. Each must be labeled
`authoritative Step 11 technical rule`, cite its Step 10 parent authority, and
state every non-claim.

Permitted decisions include:

- construction-only owner-view constraint normalization without claiming a
  physical source-to-target transform;
- deterministic faceted joining of source-unowned sectors without smoothing,
  view averaging, or physical corner pairing;
- exact internal base-course Z stations and bounded hidden overlap within the
  approved 35 cm base span;
- combined runtime packaging while retaining separate watertight component
  shells and lineage;
- planning-only classification of C-005/C-006/C-007 as geometry or later
  texture/material consumers;
- budgets, LODs, collision, paths, validators, and review cameras.

Step 11 technical rules may not modify source pixels, exact measurement
records, historical Step 10 records, N3, K80, the sixteen mappings, R3/R5/R7/
R9, direct target extrema, or any blocked exact claim.

## Production Boundary

The following remain forbidden in Step 11:

- Blender or DCC launch;
- any `.blend`, mesh, geometry, UV, texture, material, FBX, collision, LOD, or
  Unreal asset creation;
- scripts that generate or alter geometry;
- production-root creation under the reserved A005 Blender, Exports, Textures,
  Saved/Automation, or Content paths;
- rectification, averaging, smoothing, primitive fitting, projection shells,
  detached carriers, visual-fit tuning, or source-pixel warping;
- reading or importing A001-A004 asset-specific artifacts;
- source-to-source or cross-view physical pixel pairing;
- relabeling an interpretation or technical convention as source evidence;
- editing completed Step 01-Step 10 authority artifacts;
- advancing into Step 12 in this session.

## Pre-Registered Validation Gates

Step 11 passes only if all gates pass:

1. all locked inputs exist, are regular non-symlink files, and match SHA-256;
2. the Flamestrike statement and contract ID match exactly;
3. all required outputs exist and parse where structured;
4. the construction blueprint names A005 and `Static Mesh` only;
5. bounds are exactly 220, 120, 90, 140, 110, and 35 cm in their approved
   roles;
6. origin/pivot are `(0,0,0)` and Z=0 is the ground/underside station;
7. C-001 through C-004 are separate planned watertight components;
8. runtime packaging is one combined Static Mesh containing separate shells;
9. all seven semantic component IDs and all three contact IDs are accounted;
10. every planned vertex is covered by exactly one authority group;
11. every visible authority group cites exact evidence or an approved rule;
12. every inferred/hidden group cites an I10 rule and Step 11 technical rule;
13. no group claims a physical cross-view pixel pair, source center, source
    pivot, unified scale, or source-to-target transform;
14. N3 is used only as C-004 outer/bottom construction boundary;
15. K80 and the sixteen mappings are used only at CL-003 within their approved
    target-space scope;
16. R3/R5/R7/R9 are not modified or relabeled as source evidence;
17. back/right remain validation holdouts and are not construction owners;
18. source-visible contact edges remain owner-view constraints;
19. every hidden closure is independently watertight and all overlap is behind
    visible contact evidence;
20. overlap is positive, bounded, and has zero allowed visible exposure;
21. C-005/C-006/C-007 do not control unsupported primary silhouette;
22. material/UV content is planning only and Step 14 remains the authority gate;
23. LOD0 is within the approved large-prop 4k-10k target range and LOD1-LOD3
    preserve the primary silhouette first;
24. collision is planning only and does not claim source geometry;
25. all future paths use A005 and no A001-A004 production path;
26. all six required review views and orientation axes are specified;
27. blocked methods are explicit;
28. DCC, geometry, texture, FBX, collision, LOD, Unreal, and production output
    counts remain zero;
29. no reserved A005 production root is created;
30. changed tracked paths are contained by the contract allowlist;
31. source and completed Step 01-Step 10 inputs remain byte-identical;
32. the visible review packet plainly says planning only and no geometry;
33. the output record classifies the blueprint `authoritative` only after the
    complete audit passes;
34. the Step 12 handoff authorizes only later preparation of a separate Step 12
    contract after restart;
35. the final checkpoint, scoped commit, push, and remote verification pass.

## Stop Conditions

Stop and classify the result `blocked` if any input hash changes, an output
requires legacy data, an exact source claim needs invention, a planned vertex
lacks authority, a hidden overlap cannot be kept invisible, a production root
would be opened, the complete audit fails without a bounded record-only fix,
or the Git scope cannot exclude unrelated user work.

## Completion And Restart Rule

On a complete pass:

- classify the Step 11 blueprint and technical rules `authoritative`;
- resolve `S10R-BLOCK-009` only as the Step 11 planning gate for later Step 12;
- keep DCC/Unreal/production unstarted;
- visibly open the planning review packet and output record;
- checkpoint, commit only the exact allowlisted Step 11 dependency scope,
  push `main` to `assetforge`, and verify local/remote equality;
- stop for the mandatory restart without beginning Step 12.
