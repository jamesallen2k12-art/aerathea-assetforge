# A005 A02 Visual Rejection / A03 Core Recovery Record

Status: A02 visually rejected and quarantined; A03 Core Recovery active

Artifact classification: `authoritative recovery record`

Date: 2026-07-20

Recovery ID: `A005-CR-VISUAL-CORRECTION-A03`

## Flamestrike Decision

After the exact A02 final image was reopened visibly, Flamestrike determined
that it was better than A01 but that the bottom pieces remained materially
wrong and the displayed pixel colors remained off. The earlier word
`approved` was issued before the image was visible and was explicitly paused;
it was never recorded as an A02 visual approval.

A02 is therefore rejected as a visual candidate and quarantined. Its numeric,
topology, packaging, and preservation results remain `proof only` within
their exact tested scope.

## Core Recovery Assessment

- Last known Core-valid state: the unchanged authoritative source plus the
  approved Step 11 construction blueprint and Step 14 UV/material plans.
- First drift action: the A02 builder replaced C004/C003/C002 with continuous
  loft profiles and treated bounding-box and centerline band-height compliance
  as sufficient evidence of the source-owned base construction.
- Causal assumption: correct outer bounds plus three visible vertical bands
  were assumed to prove visual equivalence even though they did not test the
  reference's chunky masonry/rubble silhouette, course breaks, or spatial
  source correspondence.
- Color false pass: the A02 displayed-color gate compared global stone/red
  distributions with a stone median RGB-distance tolerance of `32`; it did
  not compare corresponding source/render regions and allowed the accepted
  render's washed and misrouted base appearance to pass.
- Known source constraint: the source drawing proportions and its printed
  physical targets have documented ratio conflicts. The exact physical
  targets still govern world bounds; Step 11 owner-view target-space profiles
  govern the shape inside those bounds.

## Affected Outputs And Classification

- A02 Blender, textures, FBXs, candidate manifest, and final image:
  `quarantined` as visual candidates; preserved in place.
- A02 `26/26` validation: `proof only` for the exact numeric/technical tests;
  its visual-equivalence implication is `invalid and superseded`.
- A02 builder, renderer, and auditor: `reference only` for A03 diagnosis.
- A02 output and review records: authoritative only as historical technical
  and rejection records after this recovery entry.
- A01 remains `quarantined`; no A01 or A02 file is overwritten or deleted.
- Unreal outputs remain `0`; `Fully game-ready` remains `false`.

## Smallest Sufficient Recovery

Return to the unchanged Step 11/14 boundary and create A03-suffixed outputs.
A03 must:

1. preserve the exact `140 x 110 x 220 cm` world bounds and `35 cm` base;
2. implement the source-owned front/left base profiles and source-critical
   silhouette breaks as faceted masonry/rubble geometry within the four-shell
   topology;
3. spatially compare orthographic source-owner proof renders against the
   normalized source panels, rather than checking extents alone;
4. verify source-color routing with a shadeless correspondence proof and
   verify the final lit render with stricter spatial color-error gates;
5. keep all intermediate attempts internal and open only the final A03 image;
6. stop before Unreal, library, or visual-canon promotion.

## Recovery Checkpoint

Pre-recovery checkpoint:
`Saved/ProjectRecovery/20260720-213755/`.
