# Step 08R Contract - Core-Reassessed Top Exact Measurement A01

Status: `approved execution boundary; output approved for bounded closeout`

Artifact classification: `authoritative for execution scope only`

Date: 2026-07-15

## Preparation Approval

- Approver: Flamestrike
- Statement: `approved`
- Date: 2026-07-15
- Approved action: prepare and visibly present one separate Core reassessment
  and renewed Step 08 authorization contract without executing Step 08
- Execution authority granted by that statement: none
- Step 08 execution approval: pending separate Flamestrike decision on this
  exact visible contract

## Core Reassessment Decision

The prior Step 08 execution boundary cannot resume by inheritance. It was
suspended by Core Recovery before any tracked Step 08 output was created, and
the original Step 07-to-Step 08 handoff remains
`quarantined/superseded`.

The approved recovery chain now provides sufficient bounded authority to
propose a renewed top-panel measurement pass:

- Step 04R supplies the authoritative recovered semantic inventory and 48
  exact discrete top-contact observations;
- Step 05 is restored within its approved convention, frame, registration,
  and blocking boundaries;
- Step 06 measurement authority exists only through the approved Step 06R and
  Step 06Q recovery overrides;
- Step 07 measurement authority exists only through the approved Step 07R
  recovery override;
- the original Step 06 and Step 07 packages remain quarantined as wholes.

If Flamestrike approves this exact contract, Step 08 may resume only as the
bounded `STEP_08R` execution defined below. No old handoff, superseded next-gate
claim, quarantined whole package, or pre-recovery top coordinate becomes
authority through this contract.

## Step Decision

Determine which formula-linked calibration observations, source-visible
footprints, outer and inner perimeters, exact pixel-set center types,
component-ownership observations, orientations, and contact relationships are
directly supported by the authoritative top panel. Preserve every occluded,
annotation-obstructed, ambiguous, inferred, or disagreement-dependent result
as blocked.

The step must produce one approval decision: accept, reject, or mark blocked a
top-view-only exact measurement package. It must not produce geometry or a
cross-view integrated dataset.

## Governing Core Principle

Evidence-Bound Decision Rule, closed-world authorization, Stop-the-line,
Core Recovery, and strict evidence-versus-interpretation separation. Exact
source pixels, approved formulas, current hashes, bounded recovery records,
and explicit blocked states control the step. Visual plausibility, legacy
results, convenient contour closure, primitive fitting, and superseded
handoffs are not authority.

## Authorizing Plan Section

`docs/projects/assetforge/BLOODAXE_CAIRNSTONE_PIXEL_EXACT_FRESH_START_MULTISTEP_PLAN.md`,
Section 13, `Step 08 - Top Exact Measurement Contracts`, together with its
Sections 4, 5, 8, and 9 evidence, measurement, contract, and completion rules.

## Execution Approval

- Approver: Flamestrike
- Statement: `approved`
- Date: 2026-07-15
- Reviewed presentation: exact visible Step 08R contract followed by the
  plain-English top-view measurement summary
- Authorized scope after approval: only the actions and writable outputs named
  below
- Exclusion: output approval, Step 09, interpretation, geometry, DCC, texture,
  export, Unreal, staging, commit, push, and every adjacent action except the
  separately conditioned post-output-approval closeout stated below

The separate execution approval satisfies this gate only for the exact scope
in this contract.

## Output Approval And Closeout Amendment

- Approver: Flamestrike
- Statement: `approved`
- Date: 2026-07-15
- Approved result: preserve the four independent top calibration
  observations and all 48 reverified contact pixels while keeping every
  filled footprint, closed perimeter, center, unified scale, origin, pivot,
  interpretation, and geometry result blocked
- Closeout expansion approved after repository audit: save and push the
  complete approved A005 recovery evidence chain required by Step 08R,
  including Dependency Audit A01, Step 06R, Step 06Q, Step 07R, Step 08R, and
  their A005 status records
- Exclusion: every unrelated worktree file, recovery-journal history,
  A001-A004 data, image rectification, Step 09 execution, interpretation,
  geometry, and production work
- Approved pre-closeout checkpoint:
  `Saved/ProjectRecovery/20260715-223831/`

This amendment authorizes dependency-complete A005-only staging, commit, and
push. It does not reclassify any quarantined original package or authorize the
later rectification itself.

## Approved Inputs After Execution Approval

1. `AGENTS.md` and the authoritative fresh-start multi-step plan.
2. A005 project charter, fresh-start data firewall, source-authority lock,
   reset/resume state, artifact index, approval log, and current Core Recovery
   status.
3. The authoritative A02 source and exact scanline evidence.
4. The authoritative Step 03 crop manifest and lossless top panel:
   `panels/STEP_03/SM_GIA_BloodAxeCairnstone_A005_STEP_03_TOP.png`.
5. The authoritative Step 04R contract, recovered semantic inventory,
   recovered top-contact manifest, and approved output record. Step 04R
   evidence and validation may be used only as `proof only`.
6. The authoritative Step 05 pixel/coordinate-frame record and
   orientation-registration manifest.
7. Step 06R, Step 06Q, and Step 07R authoritative recovery records only for
   their explicit authority boundaries, preserved disagreements, and blocker
   status. Their front/back/left/right coordinates and calibrations may not be
   reused as top-view measurements.
8. Generic measurement and validation rules containing no A001-A004 Cairn
   Stone constants or generated data.

The original Step 04 top review coordinates, original Step 06 and Step 07
whole packages, original Step 07 output `Next Gate`, and original
Step 07-to-Step 08 handoff are not approved inputs.

## Locked Top-View Registration Authority

The top panel uses zero-based integer pixel centers, `x` right and `y` down,
with image `+x` mapped to world `+X`, image `+y` mapped to world `-Y`, and
panel normal `+Z`.

The following Step 05 source-authored span endpoints are the only approved
top-view calibration endpoints:

- `XS-C001-XNEG-TOP` and `XS-C001-XPOS-TOP`: declared `120 cm` C-001
  maximum-width span;
- `XS-C004-XNEG-TOP` and `XS-C004-XPOS-TOP`: declared `140 cm` C-004 outer
  footprint-width span;
- `YS-C001-YPOS-TOP` and `YS-C001-YNEG-TOP`: declared `90 cm` C-001
  maximum-depth span;
- `YS-C004-YPOS-TOP` and `YS-C004-YNEG-TOP`: declared `110 cm` C-004 outer
  footprint-depth span.

Each pair must remain an independent calibration observation using
`pixel_span = second_axis_endpoint - first_axis_endpoint` and
`cm_per_px = declared_real_span_cm / pixel_span`. No axis, component, or
cross-view observation may be averaged, silently selected, stretched, or
substituted for another.

## Allowed Actions After Execution Approval

- create the required pre-action recovery checkpoint;
- list every input/output path, verify every authoritative input identity and
  hash, and fail closed on any mismatch;
- inspect only the authoritative top panel at native resolution for new Step
  08R measurement decisions;
- create four independent top calibration observations from the locked Step
  05 endpoint pairs and preserve every disagreement without selection;
- record exact integer, source-owned, annotation-excluded visible boundary
  pixels, ordered discontinuous boundary samples, and row-wise half-open
  source-owned spans for named components only where direct ownership is
  proven;
- keep outer contour, inner contact perimeter, visible annulus, and shared
  stacked-layer envelope records separate;
- classify every examined sector as `visible source-owned`, `occluded`,
  `annotation-obstructed`, `ambiguous`, or `blocked` without converting a
  blocked sector into closure;
- re-audit all 48 authoritative Step 04R discrete top-contact observations
  against the current top panel and use them only as discrete contact
  evidence unless additional exact source-owned observations are independently
  recorded by Step 08R;
- compute a filled-footprint pixel count or pixel-set centroid only when one
  named component has a fully source-owned closed visible boundary and every
  included row span is explicitly recorded with annotation pixels excluded;
- use only the declared exact pixel-set formula
  `P = union of recorded source-owned half-open row spans`,
  `N = |P|`, and
  `center_px = (sum(x for P) / N, sum(y for P) / N)`;
- record the result as a top-view pixel-set center, never a bounding-box
  center, raw-color-density center, physical mass center, final origin, or
  final pivot;
- leave the center blocked if the boundary is discontinuous, occluded,
  annotation-obstructed, ambiguous, shared without proven layer ownership, or
  dependent on inferred closure;
- record visible orientation observations only from exact source-owned marks
  and declared formulas, with non-zero component rotation blocked unless the
  source evidence proves it;
- create the scoped candidate manifests, one unfilled exact-mark review board,
  focused validation, and a candidate output record;
- automatically open the complete review package in visible desktop windows;
- after separate Flamestrike output approval only, create the approved
  restart handoff, update the scoped A005 status/index/approval records,
  checkpoint, and commit/push only the approved Step 08R tracked outputs.

## Scoped Writable Outputs After Execution Approval

- this contract, updated only to record exact execution approval;
- `manifests/STEP_08R_TOP_CALIBRATION_RECORD.json`;
- `manifests/STEP_08R_TOP_MEASUREMENT_MANIFEST.json`;
- `manifests/STEP_08R_TOP_PERIMETER_CENTER_MANIFEST.json`;
- `manifests/STEP_08R_TOP_SECTOR_CLASSIFICATION.json`;
- `manifests/STEP_08R_TOP_DISAGREEMENT_LIST.json`;
- `manifests/STEP_08R_VALIDATION_MANIFEST.json`;
- `evidence/STEP_08R/SM_GIA_BloodAxeCairnstone_A005_STEP_08R_TOP_MEASUREMENT_EVIDENCE.png`;
- `steps/STEP_08R_OUTPUT_RECORD.md`;
- after separate output approval only,
  `handoffs/STEP_08R_TO_STEP_09_HANDOFF.md` and scoped updates to
  `SM_GIA_BloodAxeCairnstone_A005_RESET_RESUME_STATE.md`,
  `SM_GIA_BloodAxeCairnstone_A005_ARTIFACT_INDEX.md`, and
  `SM_GIA_BloodAxeCairnstone_A005_APPROVAL_LOG.md`;
- local-only checkpoints and rejected diagnostics under `Saved/`.

No existing authoritative, proof-only, quarantined, invalid, or reference-only
artifact may be overwritten or deleted.

## Blocked Actions And Files

- using any A001-A004 asset-specific mesh, crop, coordinate, formula, script,
  manifest, texture, render, or remembered result;
- using the original quarantined Step 04 top coordinates, original Step 06 or
  Step 07 packages as wholes, superseded output next-gate text, or the original
  Step 07-to-Step 08 handoff as authority;
- altering the A02 source, exact scanline evidence, Step 03 top panel, or any
  previously approved or quarantined artifact;
- treating labels, arrows, extension lines, borders, gridlines, background,
  shadow-only pixels, or review annotations as component ownership;
- threshold-derived ownership, color-density ownership, automated segmentation
  authority, smoothing, interpolation through gaps, or primitive fitting;
- rectangular, ellipse, superellipse, bounding-box, procedural, or analytic
  footprint substitution;
- duplicating one shared contour or envelope as proof of multiple hidden
  layers;
- inventing a bottom footprint, hidden interface, source-invisible thickness,
  closed contact perimeter, occluded sector, or physical cross-view pairing;
- displaying a filled footprint, colored solution overlay, mask, smoothed
  envelope, inferred contour, candidate geometry, or any image that could be
  mistaken for geometry approval;
- selecting or averaging disputed calibration, converting blocked
  measurements to world space, or reconciling other views;
- promoting a top-view center to final origin, pivot, centerline, transform,
  snap anchor, or physical center;
- resolving `C-005`, `C-006`, `C-007`, `U-001` through `U-009`, `OS-001`
  through `OS-003`, or hidden `CL-001` through `CL-003` sectors by inference;
- interpretation, geometry, DCC, UV, texture, material, FBX, collision, LOD,
  Unreal, render, or production-root work;
- Step 09 or any later step;
- staging, commit, or push before separate output approval, or staging any
  unrelated worktree change.

## Expected Decision Outputs

1. One independent top calibration record containing four formula-linked
   observations and explicit disagreement status.
2. One top measurement manifest containing only exact source-visible,
   annotation-excluded observations.
3. One perimeter/center manifest separating outer perimeters, inner contact
   perimeters, visible annuli, shared envelopes, exact pixel sets, computed
   top-view centers, and blocked centers.
4. One sector classification preserving visible, occluded,
   annotation-obstructed, ambiguous, and blocked sectors.
5. One top-view disagreement list.
6. One unfilled source-only measurement review board.
7. One focused validation manifest and one candidate output record.

The output may pass with blocked measurements. It may not pass by filling an
unknown, selecting a convenient scale, or presenting interpretation as exact
evidence.

## Validators And Pass Conditions

- every approved input path exists and every recorded file/pixel hash matches;
- no original source, panel, approved record, quarantined record, or proof
  artifact is modified;
- all output JSON records parse and all declared references resolve;
- exactly four top calibration observations are recorded; each endpoint ID,
  pixel span, declared centimeter span, formula, axis, and exact rational or
  decimal result replays without error;
- all calibration disagreements remain explicit and no average, axis reuse,
  component reuse, or cross-view selection occurs;
- every retained coordinate is integer-valued, in bounds, and round-trips
  between top-panel-local and full-source space with `0 px` error;
- every retained ownership or perimeter observation maps to a directly
  visible source-owned pixel for its named component or contact role;
- no retained ownership observation lies on annotation, grid, border,
  background, unsupported shadow, or ambiguous pixels;
- all 48 Step 04R contact observations match the current top panel exactly and
  remain discrete unless Step 08R records separately supported additions;
- no contact, perimeter, or layer is closed across an occluded, ambiguous, or
  annotation-obstructed sector;
- each exact pixel-set calculation lists every source-owned half-open row
  span, excludes annotation pixels, reconstructs deterministically, and
  replays `N`, coordinate sums, and centroid without error;
- no pixel-set or center is produced for a boundary that is not fully
  source-owned and closed in the visible top panel;
- bounding-box, raw-color-density, primitive-fit, threshold, and smoothed
  center methods are absent;
- `C-005`, `C-006`, and `C-007` remain blocked wherever their physical
  ownership or identity is not directly proven;
- the evidence board contains untouched source pixels and exact unfilled
  points/boundaries only, with `0` changed source-tile pixels and maximum RGB
  delta `0`;
- candidate fills, masks, inferred overlays, geometry, and production
  artifacts are absent;
- A001-A004 asset-specific access is false;
- Step 09 is not started and unrelated staged files are absent;
- Flamestrike separately approves, rejects, or marks the completed output
  blocked after visible review.

## Visible Review Requirement

Before requesting output approval, automatically open in visible desktop
windows:

- the untouched authoritative top panel;
- the unfilled Step 08R measurement evidence board;
- all five candidate measurement/classification/disagreement records;
- the proof-only validation manifest;
- the candidate output record.

Links alone do not satisfy this review gate.

## Artifact Status

This contract is now `authoritative for execution scope only`. Step 08R
measurement records begin as `candidate`;
evidence and validation begin and remain `proof only`.

After separate visible output approval, only explicitly accepted exact
top-view observations, formula records, exact source-owned pixel sets, and
top-view center results become `authoritative` within their declared
boundaries. Disagreements and unsupported centers remain blocked. The original
Step 06 and Step 07 packages remain quarantined as wholes, the original Step
07-to-Step 08 handoff remains `quarantined/superseded`, and no final origin,
pivot, interpretation, geometry, production, or Step 09 authority is created.

On failure or drift, stop immediately. Preserve and classify affected new
artifacts as `invalid` or `quarantined`, record the drift when required, and
return to the last Core-valid state without repairing forward.

## Stop Conditions

Stop on any input/hash mismatch; missing formula or reference; unapproved
input; need for legacy data; annotation or background accepted as ownership;
ambiguous ownership required for completion; inferred closure; shared contour
misrepresented as distinct hidden layers; calibration disagreement requiring
selection or averaging; requested filled visual; candidate geometry;
cross-view reconciliation; blocked final-center promotion; unrelated staged
change; or any action outside this contract.

A preserved disagreement or blocked sector may remain blocked while
independent, non-conflicting exact top evidence is completed.

## Smallest Sufficient Change

Create only the top-view calibration, exact source-owned measurement,
perimeter/center, sector, disagreement, unfilled evidence, validation, and
output records required for Flamestrike to approve, reject, or mark Step 08R
blocked. Do not start cross-view integration or production.

## Context And Checkpoints

Estimated context demand: moderate to high; one native-pixel top measurement,
formula, evidence, validation, and visible-review pass with no geometry or
render loop.

If execution is approved:

- pre-action checkpoint before opening any Step 08R output path;
- recovery checkpoint immediately on any stop condition or failed validation;
- validated-candidate checkpoint before visible review;
- approved pre-closeout checkpoint before any status/index/handoff update;
- final checkpoint after approved scoped closeout and before the mandatory
  restart.

## Next Approval Gate

The Step 08R output and dependency-complete A005-only closeout are approved.
After closeout and the mandatory restart, the next permitted presentation is
a Step 09 cross-view exact-dataset audit contract. The agreed future
rectification direction remains pending that audit and a separate
interpretation contract; production work remains unauthorized.
