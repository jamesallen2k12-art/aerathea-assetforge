# A005 Scale-Authority Core Reassessment Contract A01

Status: `execution complete; visible output approved`

Artifact classification: `authoritative for execution scope only`

Authority effect: authorizes only the bounded field-level reassessment defined here

Date: 2026-07-16

## Preparation Authority

Flamestrike approved preparation of this separate Core reassessment contract after reviewing:

- `DRAFT_MULTIVIEW_NORMALIZATION_GATE_AMENDMENT_A01.md`;
- `DRAFT_A005_SCALE_AUTHORITY_REASSESSMENT_A01.md`;
- the recommended direction `A005-SR-01-B`, `A005-SR-02-A`, `A005-SR-03-A`, and `A005-SR-04-A`.

That approval establishes the reassessment direction only. It does not execute this contract, amend Core, reclassify A005 records, record a drift finding, revise Step 10, rectify images, authorize a transform, or begin geometry.

## Execution Approval

- Approver: Flamestrike
- Statement: `approved`
- Date: 2026-07-16
- Approved contract: this exact `A005-CR-SA-A01` boundary
- Approved historical-role choice: `A005-CR-ROLE-A`, retrospective source-role correction
- Authorized action: field-level Core reassessment, conditional drift/status records, validation, and visible review package named here
- Explicit exclusions: rewriting existing Step 01-Step 10 records, rectification, Step 10 revision, Step 11, geometry, generic-pipeline edits, commit, and push

## Output Approval

- Approver: Flamestrike
- Statement: `approved`
- Date: 2026-07-16
- Reviewed output: the visible `A005-CR-SA-A01` reassessment package
- Authority effect: promotes only the bounded field-level classification and status override to `authoritative for recovery routing only`
- Preparation effect: after mandatory restart, permits preparation of the separate scale-authority recovery contract only
- Explicit exclusions: recovery execution, source priority or transform application, corrected or rectified panels, Step 10 revision, Step 11, geometry, commit, and push

## Step ID And Name

`A005-CR-SA-A01` - Scale-Authority Core Reassessment A01

## One Decision

Determine the exact A005 authority impact of treating the six printed dimensions as approximate production targets rather than exact per-view pixel calibrators, then identify and record:

- the last unaffected Core-valid evidence boundary;
- the first affected assumption or authority statement;
- every directly or transitively affected record or field;
- every unaffected raw-pixel, source-identity, orientation, semantic, and contact observation that remains usable;
- whether this is confirmed historical drift or a prospective direction change;
- the smallest sufficient recovery action required before Step 10 can resume.

This reassessment does not perform that recovery.

## Governing Core Principles

- First Principles: distinguish the actual source role from the role previously assigned to it.
- Evidence-Bound Decision Rule: preserve source pixels, raw coordinates, hashes, and approved user decisions.
- Stop-the-line: Step 10, rectification, interpretation approval, Step 11, and production remain stopped.
- Core Recovery: do not repair forward; identify the valid boundary and classify affected outputs first.
- Kaizen: preserve every valid field and replace only the smallest affected authority layer.
- Time-Is-Value: use one indexed override instead of rewriting seventy mixed records during diagnosis.

## Exact Authorizing Direction

The approved reassessment direction is:

- `A005-SR-01-B`: printed dimensions are approximate production targets;
- `A005-SR-02-A`: front is the candidate X/Z owner and left is the candidate Y/Z owner;
- `A005-SR-03-A`: the candidate top relative X/Y mapping is the front/left factor, `1.1260059213` or `+12.60059213%` X relative to Y;
- `A005-SR-04-A`: future production use must transform original coordinates analytically and keep normalized images derivative/review-only.

For this reassessment, those choices control impact analysis only. They do not yet control construction, corrected panels, world conversion, or geometry.

## Historical-Drift Decision Required By This Contract

The prior conversation described the original centimeter labels as possibly general and suggestive. Flamestrike then approved `A005-SR-01-B` with that condition visible.

The reassessment must classify the change as exactly one of:

### `A005-CR-ROLE-A` - Retrospective Source-Role Correction

The labels were always intended as approximate production targets, and the earlier exact-calibrator classification misunderstood the source. The first affected authority statement is historical drift and must be recorded under Core Recovery.

Candidate recommendation: `A005-CR-ROLE-A`.

### `A005-CR-ROLE-B` - Prospective Direction Change

The labels were correctly treated as exact when approved, but Flamestrike is now changing their role for future production. Earlier work remains historically valid under its former authority, while downstream authority must be superseded prospectively.

### Stop Rule

If the approved execution contract does not identify `A005-CR-ROLE-A` or `A005-CR-ROLE-B`, execution must stop before writing a Drift Ledger entry or status override.

## Approved Inputs For Future Execution

### Governing Inputs

- `AGENTS.md`;
- `docs/projects/assetforge/BLOODAXE_CAIRNSTONE_PIXEL_EXACT_FRESH_START_MULTISTEP_PLAN.md`;
- `docs/projects/assetforge/AETHERFORGE_BLUEPRINTS.md`;
- `docs/assets/CONCEPT_ART_TO_GAME_READY_ASSET_PIPELINE.md`;
- this exact approved contract after it is copied to the tracked A005 step path;
- Flamestrike's recorded contract approval and `A005-CR-ROLE` choice.

### A005 Audit Universe

Read-only inspection is permitted for every `*.md` and `*.json` record under:

`docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/`

This directory-and-extension set is the complete A005 record audit universe for this reassessment. PNGs may be hash-checked or visibly inspected only when a record's classification cannot be decided from its source links and fields. They must not be altered.

### Required Evidence Inputs

- authoritative A02 source and Step 03 panels;
- Step 02 source lock and Step 03 crop manifest;
- current A005 reset/resume state, artifact index, and approval log;
- Step 04R recovered component/contact authority;
- Step 05 coordinate-frame and orientation-registration records;
- Step 06Q, Step 06R, and Step 07R bounded recovery records;
- Step 08R top records;
- four Step 09 authoritative decision manifests and Step 09 output/handoff;
- pending candidate Step 10 contract, registry, manifests, and output record;
- A01 pixel-only consistency metrics;
- A02 pixel-only normalization metrics;
- approved local multiview amendment and A005 reassessment direction package.

### Input Lock

Before opening any output path, execution must enumerate every input path, size, modification time, and SHA-256 hash. Any mismatch with the contract-preparation hashes must stop execution unless the changed file is explicitly reviewed and the input lock is renewed by Flamestrike.

## Allowed Future Execution Actions

1. Enumerate and hash the complete A005 Markdown/JSON audit universe.
2. Search every record for direct-dimension, calibration, centimeter-conversion, world-frame, cross-view, Step 10, and downstream dependency claims.
3. Classify at JSON-pointer or named-heading granularity when one record contains both valid raw evidence and affected authority.
4. Trace direct and transitive dependencies without changing the source records.
5. Determine the last unaffected Core-valid boundary and first affected assumption.
6. Classify `A005-CR-ROLE-A` or `A005-CR-ROLE-B` exactly as approved with the contract.
7. Create the bounded input index, affected-record index, classification record, validation, status record, output record, and same-step recovery handoff named below.
8. Update the reset state, artifact index, and approval log only to reflect the reassessment stop and candidate classifications.
9. If and only if `A005-CR-ROLE-A` is approved and confirmed, append one bounded entry to the Drift Ledger and the new A005 reassessment status record.
10. Open the completed reassessment package visibly for Flamestrike review.

## Blocked Future Execution Actions

- modifying original source pixels, scanline evidence, Step 03 panels, or any proof image;
- editing any existing Step 01 through Step 10 contract, manifest, output record, evidence board, or handoff;
- deleting, moving, or overwriting any existing A005 record;
- rewriting centimeter values or calibrations in place;
- treating A01/A02 metrics or normalized images as authority;
- recording front/left ownership as construction authority;
- applying the 12.60059213% top transform;
- creating a normalized or rectified production panel;
- choosing absolute X, Y, or Z production dimensions;
- component measurement, world conversion, closure, center, origin, pivot, snap-anchor, topology, or geometry work;
- approving or revising `I10-001` through `I10-010`;
- beginning Step 11;
- editing `AGENTS.md`, the generic pipeline, AetherForge Blueprints, or the fresh-start multistep plan;
- using A001-A004 asset-specific records;
- staging, commit, push, or closeout before visible output approval.

## Exact Allowed Future Write Paths

Execution may create:

- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/steps/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_CONTRACT.md`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_INPUT_INDEX.json`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_AFFECTED_RECORD_INDEX.json`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_CLASSIFICATION.json`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_VALIDATION.json`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/steps/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_OUTPUT_RECORD.md`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/handoffs/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_TO_RECOVERY_HANDOFF.md`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/SM_GIA_BloodAxeCairnstone_A005_CORE_REASSESSMENT_STATUS_20260716_SCALE_AUTHORITY.md`.

Execution may update only:

- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/SM_GIA_BloodAxeCairnstone_A005_RESET_RESUME_STATE.md`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/SM_GIA_BloodAxeCairnstone_A005_ARTIFACT_INDEX.md`;
- `docs/assets/blueprints/SM_GIA_BloodAxeCairnstone_A005/SM_GIA_BloodAxeCairnstone_A005_APPROVAL_LOG.md`;
- `docs/projects/assetforge/DRIFT_LEDGER.md`, only under the conditional rule above.

Local checkpoints and transient validator logs may be created only under `Saved/ProjectRecovery/` and `Saved/AssetForgeResearch/A005_ScaleAuthority_CoreReassessment_A01/`.

Any other write path is forbidden and must stop execution.

## Required Field-Level Classifications

Every affected record entry must identify:

- path and locked hash;
- current artifact status;
- exact JSON pointer or Markdown heading;
- direct or transitive dependency;
- source fact or formula being reassessed;
- whether it depends on labels as exact calibrators;
- whether it remains valid as raw pixel evidence;
- whether it remains valid as source-authored approximate-target evidence;
- whether its physical/world conclusion remains valid;
- new candidate classification;
- required recovery consumer;
- reason and evidence;
- whether an in-place rewrite is blocked.

Allowed classifications:

- `unaffected_authority`;
- `valid_raw_evidence_only`;
- `valid_approximate_target_evidence_only`;
- `partial_mixed_record_requires_override`;
- `superseded_world_or_calibration_conclusion`;
- `quarantined_dependency`;
- `invalid_if_historical_drift_confirmed`;
- `reference_only_pending_recovery`;
- `candidate_step10_requires_revision`.

No classification becomes final authority until Flamestrike approves the visible reassessment output.

## Required Decision Rules

### Preserve Raw Evidence

Source hashes, source pixels, lossless crops, integer coordinates, pixel spans, annotation text, annotation endpoints, view labels, orientation marks, semantic component IDs, and exact discrete contacts remain evidence unless a separate defect is proven.

### Separate Formula From Premise

A formula may replay exactly while its premise changes. The arithmetic is preserved as reproducible historical evidence; the resulting world or authority conclusion must be separately classified.

### Mixed Records

Do not quarantine a whole mixed record when exact pointers/headings can preserve valid evidence safely. If field-level separation cannot be proven, classify the whole record conservatively and report the block.

### No Repair Forward

Do not revise Step 10 or measurement records during reassessment. The output handoff must define a later recovery contract.

### No Diagnostic Promotion

A01/A02 establish diagnostic evidence and the approved direction. They do not become source authority, construction contours, physical scale, or geometry.

## Expected Future Execution Outputs

1. Complete hash-locked input index.
2. Complete field-level affected-record index.
3. Core classification record identifying last valid boundary and first affected assumption.
4. Explicit historical-drift or prospective-change result.
5. One bounded status record and conditional Drift Ledger entry.
6. Updated reset, artifact, and approval indexes that stop Step 10 and Step 11.
7. Focused validation manifest.
8. Output record listing every file and classification.
9. Same-step recovery handoff naming the smallest sufficient next contract.

## Validators

The execution validator must prove:

- every permitted input was enumerated and hashed before writes;
- no blocked legacy source was read;
- every A005 Markdown/JSON record was considered exactly once in the universe index;
- every lexical dimension/calibration match was classified;
- every Step 06, Step 07, Step 08R, Step 09, and pending Step 10 dependency was covered;
- direct versus transitive impact is explicit;
- raw pixels and coordinates were not silently invalidated;
- exact formulas were distinguished from changed premises;
- no original Step 01-Step 10 record was modified;
- only exact allowed write paths changed;
- the role decision is exactly `A005-CR-ROLE-A` or `A005-CR-ROLE-B` as approved;
- any Drift Ledger entry obeys the role decision and Core Recovery;
- A01/A02 remain `proof only`/`reference only`;
- no source-priority, top transform, absolute scale, rectification, interpretation, or geometry authority was created;
- Step 10 and Step 11 remain stopped;
- output hashes and artifact statuses are complete;
- the visible review package opens successfully.

## Stop Conditions

Stop immediately if:

- an input hash changed unexpectedly;
- the historical-drift role is not explicitly approved;
- an affected field cannot be traced to source and dependency records;
- a mixed record cannot be safely classified at field level;
- a required write falls outside the allowlist;
- execution would need to rewrite source evidence or an existing authority record;
- a generic-process amendment is required to complete the A005 classification;
- A01/A02 evidence conflicts with its recorded hashes or metrics;
- the reassessment starts solving scale, contours, closure, or geometry instead of classifying authority.

On stop, preserve partial outputs as `quarantined` or `proof only`, checkpoint, and report the exact block. Do not repair forward.

## Artifact Status On Pass

Before visible output approval:

- execution contract copy: `authoritative for execution scope only`;
- input index and validation: `proof only`;
- affected-record index, classification, status, output record, reset/index/log updates, and handoff: `candidate`;
- Drift Ledger entry, if conditionally created: `candidate recovery record pending Flamestrike confirmation`;
- all existing A005 artifacts retain their current status until the output decision.

After visible Flamestrike approval, the bounded classification and status override may become `authoritative` for recovery routing only. They still do not authorize record rewriting, rectification, Step 10 decisions, Step 11, or geometry.

## Artifact Status On Failure

- valid diagnostic logs: `proof only`;
- incomplete indexes or classifications: `quarantined`;
- any output created outside the contract: `invalid` and preserved for Core review;
- current A005 authority remains unchanged;
- Step 10 and Step 11 remain stopped.

## Visible Review Requirement

Open automatically:

- the output record;
- the affected-record summary;
- the Core classification record;
- the status override;
- the validation manifest;
- the conditional Drift Ledger entry, if one exists.

Clickable paths alone do not satisfy review visibility.

## Smallest Sufficient Change

Create one field-level override/index and stop. Do not rewrite seventy mixed historical records and do not create the eventual normalization transform.

## Estimated Context Demand

High. This must be one complete agent session under the approved A005 context thresholds:

- 200k warning;
- 220k handoff reserve;
- 240k mandatory stop.

If the record universe cannot be classified safely within one session, stop with a same-step incomplete handoff. Do not split the audit silently or advance into recovery.

## Planned Checkpoints For Future Execution

1. before any output path opens;
2. after the complete hash-locked input index;
3. after field-level classification and before status/ledger writes;
4. after validation and before visible review;
5. after Flamestrike's output decision and before any scoped closeout.

## Execution Approval Question

Approve this exact `A005-CR-SA-A01` execution contract together with one historical-role choice:

- `A005-CR-ROLE-A` - retrospective source-role correction; or
- `A005-CR-ROLE-B` - prospective direction change.

Recommended approval statement:

`Approve A005-CR-SA-A01 with A005-CR-ROLE-A. Execute only the field-level Core reassessment, conditional drift/status records, validation, and visible review package named in the contract. Do not rewrite existing records, rectify images, revise Step 10, or begin geometry.`
