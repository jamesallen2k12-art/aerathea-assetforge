# A005 S10R-006-R1-A Normalized Primary-Owner Bridge Execution Contract

Status: `candidate execution contract prepared; execution not approved`

Artifact classification: `candidate`

Contract ID: `A005-CR-S10R-006-R1-A-NPOB-EXEC-A01`

Prepared: 2026-07-17

## Plain-English Purpose

This contract proposes one record-only implementation and technical audit of
the approved bounded interpretation rule
`S10R-006-R1-A — Normalized Primary-Owner Bridge`.

A later, separately approved execution would map only the twelve approved
front/left construction-owner trace identities into a dimensionless common
frame. It would preserve the fourteen back/right traces as validation-only
holdouts. It would create no physical source-to-target transform, cross-view
pixel pair, target-space geometry coordinate, field sample, fill, surface,
topology, mesh, DCC artifact, or Unreal artifact.

This preparation pass records the approved rule, prepares this candidate
contract, opens it visibly, and stops. It does not execute the mapping.

## Preparation Authority

The authority chain for this preparation is exact:

1. approved S10R-006-A technical result
   `blocked_source_authority_missing` proved that N3/K80 analytic containment
   passes while all 26 upright traces lack source-view-to-target authority;
2. Flamestrike delegated the recovery-route decision with
   `you make the decision based on evidence ... and present it to me for approval`;
3. Codex selected `S10R-006-R1-A — Normalized Primary-Owner Bridge` and
   presented its exact rule, limits, and approval scope;
4. Flamestrike answered `yes approved`; and
5. that approval authorized only the bounded decision record and preparation
   of this separate visible execution contract.

It did not authorize execution.

## Step ID And Name

- Step ID: `S10R-006-R1-A-NPOB-EXEC-A01`
- Name: `Normalized Primary-Owner Bridge mapping implementation and audit`
- Asset: `SM_GIA_BloodAxeCairnstone_A005`
- Phase: bounded Step 10 interpretation recovery

## Governing Decision

The authoritative decision record is:

`manifests/S10R_006_R1_A_NORMALIZED_PRIMARY_OWNER_BRIDGE_DECISION_REGISTRY.json`

Its approved classification is:

`authoritative for bounded recovery routing and normalized common-frame interpretation only`

The prior result `blocked_source_authority_missing` remains authoritative as
the completed technical finding. This rule does not overturn that evidence or
claim that missing source correspondence was recovered. It creates a bounded
interpretation route because the approved Step 10 gate exists to decide
unavoidable construction unknowns explicitly.

## One Later Execution Decision

Later execution must produce exactly one of these bounded results:

1. `pass_normalized_primary_owner_bridge_ready_for_Flamestrike_decision` if
   all locked inputs, twelve construction mappings, ordering, handedness,
   holdout, scope, and validation gates pass;
2. `blocked_primary_owner_mapping_rule_failure` if the twelve mappings cannot
   be created exactly under the approved symbolic rule without duplicate,
   reversal, crossing, or undeclared assumption;
3. `blocked_validation_holdout_explicit_contradiction` if a back/right
   validation trace proves an explicit contradiction in side, ordering,
   handedness, or non-crossing behavior; or
4. `blocked_integrity_or_scope_failure` if any hash, write-scope,
   classification, visibility, or no-field/no-geometry gate fails.

A pass would produce a candidate implementation of the approved rule for
visible Flamestrike review. It would not promote physical correspondence,
implement S10R-006-A, close Step 10, or authorize Step 11.

## Last Known Core-Valid State

- N3: `abs(x / 70)^3 + abs(y / 55)^3 = 1`, abstract target boundary only.
- K80: `abs(x / 56)^3 + abs(y / 44)^3 = 1`, abstract target boundary only.
- Exact homothetic scale: `0.8`; strict containment value: `0.512`.
- Approved top target mappings: `16`, unique and endpoint-exclusive.
- Approved upright trace formulas: `26`, unchanged.
- Construction-owner traces: front `6` plus left `6` = `12`.
- Validation-only traces: back `6` plus right `8` = `14`.
- Source-view-to-target correspondences: `0`.
- Physical cross-view pairs: `0`.
- Explicit boundary mismatches: `0`.
- Field samples, fills, surfaces, topology, and geometry: `0`.
- `S10R-BLOCK-006`, `S10R-BLOCK-008`, and `S10R-BLOCK-009`: active.

## Exact Approved Mapping Rule

### Construction Owners

Only these twelve trace IDs may enter the normalized construction mapping:

- front primary XZ: `F-BTIR-01` through `F-BTIR-06`;
- left primary YZ: `L-BTIR-01` through `L-BTIR-06`.

No back or right trace may create an independent construction coordinate.

### Validation Holdouts

These fourteen trace IDs remain holdouts:

- back validation-only: `B-BTIR-01` through `B-BTIR-06`;
- right validation-only: `R-BTIR-01` through `R-BTIR-08`.

They may test only their recorded view role, side classification, within-side
ordering, Step 05 handedness, non-crossing result, unchanged endpoints, and
explicit contradiction. Missing physical correspondence is not itself a
holdout failure because that absence is already the approved technical fact.

### Dimensionless Common Frame

Each construction mapping record must contain only:

- exact trace ID;
- owner view and owner plane;
- registered side;
- within-side order derived deterministically from the frozen trace ledger;
- unchanged symbolic source trace formula
  `p(t) = (1-t) * p_i + t * p_o`;
- symbolic trace domain `t in [0,1]`;
- normalized transition identity `v = t`;
- inner boundary ID K80 at `t = 0`, `v = 0`;
- outer boundary ID N3 at `t = 1`, `v = 1`; and
- exact Step 05 orientation/handedness provenance.

The mapping is a symbolic, dimensionless interpretation frame. It is not an
XY, XZ, YZ, XYZ, centimeter, world, source-panel, pivot, anchor, surface, or
geometry coordinate frame.

### Ordering Rule

Within each primary owner view and each registered side:

1. replay the frozen outer-anchor row and trace IDs without modifying them;
2. sort by the source image row convention already declared by Step 05;
3. require three unique traces per side;
4. assign deterministic within-side order `1`, `2`, `3`;
5. preserve the source order only; do not convert the rank into a physical
   distance, height, angle, arc length, or centimeter value; and
6. stop if any trace is duplicated, omitted, reversed, or ambiguous.

### Boundary Identity Rule

The execution may register symbolic boundary identity only:

- `t = 0` / `v = 0` -> `C003_TSIB_K80_MEDIUM_APRON`;
- `t = 1` / `v = 1` -> `TOP_C004_OPIR_N3_ROUNDED_OVAL`.

It may replay the exact equations and homothetic containment proof. It may
not calculate an upright target coordinate, pair an upright source pixel to a
top source pixel, or declare a physical seam position.

## Locked Governing Inputs At Preparation

| Input | SHA-256 |
|---|---|
| `AGENTS.md` | `5d2d2637a58113f0a6f5ec92e40d8bb2bda9e03fb58b4868a0d9dfbb3b57ad55` |
| fresh-start multistep plan | `ba6784498d792dc85dd431c807f59620d6851af97b4cd15efe89c44a397b10b6` |
| Step 05 pixel-coordinate frame | `ccfce6b8316369b512ff83eee11fd4385dd15b546f7b92da9e80b9e7ed959d1d` |
| Step 05 orientation registration | `b7efa865e8b82f63e714d36ed4935fc02515ecc3f4d355941467f20b49348395` |
| Step 10R decision registry | `f60634468c2d820e230da0a847a181be90893411c3e7838283eb61730e7d37e5` |
| Step 10R transition registry | `3143f233c471c823693d8539f7abf7478340bdcfdd3eef871f68cabc7ca3728f` |
| frozen 26-trace ledger | `c30fe9e7ae2c623122865102b16922931e42e87e2a3bfadb145cbe73f4861113` |
| approved top mapping decision registry | `05ddf7d67cbed5bca4eb32ec749767b43f74af69bcc77f556e08cec5fb288ff2` |
| S10R-006-A blocked-result decision registry | `609a912568ccea01f65a4ec6060c53a97b93679d5a647125bed835bd9b28759b` |
| S10R-006-A remaining-block record | `5d3458789dd0317d66493b137225dcd604d05ff5c73483b4df8946bea453eb07` |

The future execution must also lock the current decision registry, this
contract, reset/resume state, approval log, artifact index, branch, HEAD,
staged-path count, and its approved pre-action checkpoint. Hashes for files
changed by this preparation pass must be captured only after preparation is
complete.

## Allowed Future Execution Outputs

After separate exact execution approval, writes are limited to:

- one input-lock manifest;
- one twelve-record normalized primary-owner mapping ledger;
- one fourteen-record validation-holdout audit;
- one independent validation manifest;
- one unfilled evidence-versus-interpretation review board;
- one technical output record;
- one decision handoff;
- one deterministic builder and one independent auditor under `Tools/DCC/`;
- the three A005 status/index records for the exact result only; and
- checkpoint metadata through the approved checkpoint tool.

Every output name must carry the exact
`S10R_006_R1_A_NORMALIZED_PRIMARY_OWNER_BRIDGE_A01` identifier. No existing
Step 10, Step 10R, S10R-003-A, S10R-006-A, source, panel, or evidence record
may be rewritten.

## Pre-Registered Technical Gates

Future execution may pass only if:

1. every locked input hash matches before output writes;
2. this visible contract hash and the later approval statement match;
3. branch and HEAD match the approved pre-action state and no path is staged;
4. all 26 frozen trace IDs, endpoints, formulas, view roles, and sides replay
   unchanged;
5. the construction set contains exactly six front and six left traces;
6. the holdout set contains exactly six back and eight right traces;
7. each primary view contains exactly three unique traces on each side;
8. all twelve construction mappings have unique mapping IDs;
9. all twelve records preserve the exact source trace ID and formula;
10. every record stores symbolic `t in [0,1]` and exact identity `v = t`;
11. every inner boundary identity is K80 at `t = 0`;
12. every outer boundary identity is N3 at `t = 1`;
13. Step 05 handedness is replayed rather than assumed;
14. no within-side order reversal or ambiguity exists;
15. no primary-owner trace crossing is introduced;
16. all fourteen holdouts remain validation-only;
17. any explicit holdout contradiction is reported and stops promotion;
18. no physical cross-view pair or source-to-target transform is created;
19. no target XY/XYZ, centimeter, pivot, anchor, field, fill, surface,
    topology, or geometry record is created;
20. original source, panels, mappings, traces, prior decisions, and physical
    targets remain unchanged;
21. all new paths are inside the approved execution allowlist;
22. the review board separates source evidence from normalized
    interpretation and shows no fill or source-target overlay;
23. the technical result is classified only within this contract's bounded
    role; and
24. execution stops for visible Flamestrike review and mandatory restart.

## Explicitly Blocked Methods And Work

- proof-only scale transforms as correspondence authority;
- physical cross-view pixel pairing;
- `S10R-003-B` revival;
- source-panel normalization by width, height, center, or bounding box;
- nearest matching, visual alignment, fitting, snapping, averaging, or
  post-result tolerance;
- affine, projective, perspective, cage, lattice, spline, or local warp;
- target trace coordinates or physical seam placement;
- field sampling, rasterization, filling, shading, extrusion, or meshing;
- CL-002 closure, C-003 annulus, hidden interface, footprint, surface,
  topology, or geometry;
- resolving `S10R-BLOCK-006`, `S10R-BLOCK-008`, or `S10R-BLOCK-009` inside
  the technical execution without a later output decision;
- Step 10 closeout or Step 11;
- DCC, Blender scene, FBX, texture, material, LOD, collision, Unreal, or
  production work;
- staging, commit, or push; and
- continuing into another step in the same session.

## Stop State And Approval Boundary

This contract is prepared and ready for visible review. It is not approved
for execution.

Execution requires a separate exact Flamestrike response to this question:

> Do you approve execution of contract
> `A005-CR-S10R-006-R1-A-NPOB-EXEC-A01` exactly as written, limited to the
> twelve-record normalized mapping, fourteen-record holdout audit,
> independent validation, unfilled visible review package, and mandatory
> stop, with no field, geometry, downstream work, staging, commit, or push?
