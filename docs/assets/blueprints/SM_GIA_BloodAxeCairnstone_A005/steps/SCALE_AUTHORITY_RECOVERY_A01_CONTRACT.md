# A005 Scale-Authority Recovery Contract A01

Status: `candidate contract; preparation approved; execution not approved`

Artifact classification: `candidate`

Authority effect: none until Flamestrike explicitly approves this exact execution boundary and the physical-bounds choice below

Date: 2026-07-17

## Plain-English Purpose

The source drawing is still the visual evidence, but its printed centimeter labels are approximate design targets rather than exact measuring rulers for each view.

This proposed recovery pass would:

1. record the intended final size targets separately from the source pixels;
2. keep front and left as the primary upright shape views;
3. create one global coordinate transform for each original panel without locally bending, stretching, or repainting individual parts;
4. widen only the derivative top review frame in X by the already approved candidate factor `1.1260059213` relative to Y;
5. show Flamestrike exactly what changed and how much distortion or disagreement remains;
6. create new recovery-suffixed records for only the scale/world conclusions that the evidence supports;
7. stop for visible approval before revising Step 10 or making geometry.

Approval of this contract would authorize that evidence-and-recovery package only. It would not approve the resulting transformed frame, Step 10 decisions, or geometry in advance.

## Preparation Authority

Flamestrike approved preparation and visible presentation of this candidate contract on 2026-07-17.

That approval permits only this contract file to be created and opened for review. It does not authorize recovery execution.

## Step ID And Name

`A005-CR-SAR-A01` - Scale-Authority Recovery A01

## One Decision

Produce a visibly reviewable candidate answer to this question:

> Can A005 be placed into one honest, original-coordinate construction frame using the approved physical targets, front/left source ownership, and one global transform per panel, while preserving every remaining source disagreement and without introducing local warping or geometry?

Possible output decisions are:

- `approve recovered frame`;
- `reject recovered frame`;
- `blocked by unresolved source/target conflict`.

## Governing Core Principles

- First Principles: separate visual shape authority, physical-size intent, and projection/scale authority.
- Evidence-Bound Decision Rule: use the approved A005 classification, original source pixels, hashes, raw coordinates, and explicit Flamestrike choices.
- Stop-the-line: Step 10 revision, Step 11, geometry, and production remain stopped.
- Core Recovery: create new recovery records; do not repair affected history in place.
- Kaizen: replace only the affected scale/world authority layer.
- Time-Is-Value: use global analytic transforms and a bounded recovery override rather than rewriting the complete project history.

## Authorizing Records

- `AGENTS.md`;
- `docs/projects/assetforge/BLOODAXE_CAIRNSTONE_PIXEL_EXACT_FRESH_START_MULTISTEP_PLAN.md`;
- `docs/projects/assetforge/AETHERFORGE_BLUEPRINTS.md`;
- `docs/assets/CONCEPT_ART_TO_GAME_READY_ASSET_PIPELINE.md`;
- `SM_GIA_BloodAxeCairnstone_A005_CORE_REASSESSMENT_STATUS_20260716_SCALE_AUTHORITY.md`;
- `steps/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_OUTPUT_RECORD.md`;
- `manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_AFFECTED_RECORD_INDEX.json`;
- `manifests/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_CLASSIFICATION.json`;
- `handoffs/CORE_REASSESSMENT_SCALE_AUTHORITY_A01_TO_RECOVERY_HANDOFF.md`;
- Flamestrike's later approval of this exact contract and one physical-bounds choice.

## Controlling Evidence State

- Last whole-step unaffected boundary: Step 04R recovered semantic/contact evidence.
- Step 05: mixed; coordinate spaces, axes, orientation marks, semantic IDs, and blocked correspondence findings remain usable.
- First affected action: Step 06 exact centimeter-per-pixel calibration from printed labels.
- Original A02 source, scanline identity, Step 03 panels, raw integer pixel coordinates, spans, visible rows, landmarks, contacts, hashes, and blocked findings remain preserved within their approved bounds.
- Printed values are approximate production-target evidence, not exact per-panel calibration premises.
- Exact centimeter-per-pixel results and dependent world conclusions from Steps 06-09 are superseded or recovery-bound.
- The current Step 10 package remains `candidate; revision required` and is not an input authority for this recovery.

The affected-record index contains:

- `101` A005 Markdown/JSON records in the locked universe;
- `70` records with direct dimension/calibration matches;
- `883` classified field or line matches;
- `63` affected/mixed records routed to this recovery contract;
- `7` candidate Step 10 records reserved for a later Step 10 revision contract.

## Required Physical-Bounds Choice

Execution may begin only after Flamestrike approves exactly one choice.

### `A005-SAR-BOUNDS-A` - Use The Printed Values As Final Production Targets

Record these as the final physical design targets:

| Target | Production value | Role |
|---|---:|---|
| Complete asset overall height | `220 cm` | absolute Z size target |
| C-001 maximum width | `120 cm` | primary monolith X size target |
| C-001 maximum depth | `90 cm` | primary monolith Y size target |
| C-004 footprint width | `140 cm` | base X size target |
| C-004 footprint depth | `110 cm` | base Y size target |
| Authored base height | `35 cm` | C-004 Z size target |

These values become production design targets, not retroactive proof that the existing panel layouts were metrically exact.

The recovery must report every mismatch between these targets and the globally transformed source evidence. It may not locally warp a component to make the numbers appear to agree.

Candidate recommendation: `A005-SAR-BOUNDS-A`.

Reason: this preserves Flamestrike's intended gameplay scale and the source-authored size language while keeping the source-role correction honest.

### `A005-SAR-BOUNDS-CUSTOM` - Flamestrike Supplies Different Final Targets

Execution remains blocked until all six replacement values and their component roles are written into the approved contract.

### `A005-SAR-BOUNDS-BLOCK` - Leave Physical Scale Blocked

Do not execute recovery. Preserve the current stop state.

## Approved Direction Proposed For Execution

Approval of this contract with `A005-SAR-BOUNDS-A` would promote the previously approved direction only into this recovery execution boundary:

- front owns the primary X/Z upright silhouette;
- left owns the primary Y/Z upright silhouette;
- back and right are validation views and retain ownership of their non-conflicting source-visible details;
- the derivative top review mapping uses X/Y factor `1.1260059213`, meaning X is expanded `12.60059213%` relative to Y;
- the top transform implementation is X expansion with Y held at `1.0` in the normalized review frame;
- every transform is global to its complete panel;
- transforms are applied analytically to original coordinates;
- original source and panel files remain unchanged;
- derivative raster panels are review artifacts only.

The diagnostic pair-mean factor `1.18138833` is not permitted. No opposing-view average becomes shape or geometry authority.

## Coordinate And Transform Rules

Every panel transform record must contain:

- source and panel path, size, and SHA-256 hash;
- original full-source and crop-local coordinate definitions;
- axis interpretation;
- mirror/orientation state;
- one global 3x3 homogeneous 2D transform matrix;
- one exact inverse matrix;
- global X and Y scale factors;
- registration translation and its non-physical review purpose;
- approved owner or validation role;
- target-size role, if any;
- permitted and blocked consumers;
- numeric round-trip error;
- holdout residuals;
- distortion summary.

For the top derivative review frame:

`x_normalized = 1.1260059213 * x_original + tx`

`y_normalized = 1.0 * y_original + ty`

`tx` and `ty` may only keep the complete transformed crop inside the review canvas. They do not establish a physical center, origin, pivot, or snap anchor.

Front, back, left, and right may use only a single global affine scale/orientation/translation per complete panel. No rotation may be introduced unless an approved source orientation mark requires it and the exact angle is recorded. No perspective, projective, local, component, row, feature, cage, lattice, spline, or content-aware warp is allowed.

## Physical Target Versus Source Shape Rule

The physical-bounds manifest and the source-coordinate transform manifest are separate authorities.

- Physical bounds state intended production size.
- Original source pixels state visible design evidence.
- Global panel transforms state how source evidence is compared across views.
- A global transform mismatch does not authorize local repair.
- A target/source conflict must remain explicit for later Step 10 interpretation.

The recovery may promote a world-space conclusion only when its source owner, transform, formula, and target role are all explicit and non-conflicting.

If the six physical targets cannot coexist with the globally transformed source evidence without local warping, the recovery output must be `blocked by unresolved source/target conflict`. It must not choose geometry.

## Approved Read-Only Inputs For Future Execution

### Governing And Recovery Records

- every authorizing record named above;
- current A005 reset/resume state, artifact index, and approval log;
- reassessment input index, affected-record index, classification, validation, status, output, and handoff;
- the exact 63 records routed to `later_scale_authority_recovery_contract` by the affected-record index.

### Source And Preserved Evidence

- authoritative A02 source and scanline evidence;
- authoritative Step 03 lossless panels and crop manifest;
- Step 04R recovered semantic/contact records;
- preserved Step 05 coordinate/orientation fields named by the reassessment classification;
- preserved raw integer coordinates, spans, rows, contacts, and landmarks from Steps 06-08R named by the classification;
- Step 09 provenance, hashes, valid semantic-role groups, and blocked findings named by the classification.

### Diagnostic Support Only

- `Saved/AssetForgeResearch/A005_PixelOnlyConsistency_A01/`;
- `Saved/AssetForgeResearch/A005_PixelOnlyNormalization_A02/`;
- `Saved/AssetForgeResearch/MultiviewNormalization_ProcessAmendment_A01/`.

These local diagnostics remain `proof only` or `reference only`. Their masks, normalized rasters, pair means, and inferred shapes cannot become authority.

### Blocked Inputs

- all A001-A004 asset-specific files and remembered values;
- the seven existing Step 10 candidate records as decision authority;
- old Cairn Stone geometry, masks, builders, textures, UVs, materials, exports, Unreal assets, and renders;
- web references or unrelated concept art;
- any diagnostic fill, bounding box, center, or silhouette treated as geometry.

## Input Lock

Before any execution output opens, the runner must enumerate and hash every permitted input actually used.

Any unexpected path, hash, size, modification time, schema, status, or approval mismatch must stop execution before writes.

## Allowed Future Execution Actions

1. Create a pre-action checkpoint.
2. Lock and hash the exact governing, source, panel, preserved-field, and diagnostic inputs used.
3. Record the approved physical-bounds choice without changing historical measurement records.
4. Build one original-coordinate global transform record per front, back, left, right, and top panel.
5. Generate derivative nearest-neighbor review panels from the original Step 03 crops.
6. Generate an orientation-matched source/derivative comparison board with source and derivative regions visibly labeled.
7. Measure and record opposing-view holdout residuals and top cross-axis distortion without using holdouts as fitting inputs.
8. Recompute only affected scale/world/integration conclusions into new recovery-suffixed records.
9. Preserve unsupported or conflicting conclusions as blocked rather than forcing a pass.
10. Create focused validation, an output record, and a handoff to a later Step 10 revision contract.
11. Open the contract outputs and review board visibly for Flamestrike.
12. Stop for Flamestrike's output decision.

## Blocked Future Execution Actions

- changing the A02 source, scanline proof, Step 03 crops, or any original panel;
- editing any existing Step 01-Step 10 contract, manifest, output, handoff, or evidence file in place;
- selecting different physical bounds from the approved choice;
- treating the printed labels as exact historical pixel-calibration premises;
- promoting A01/A02 diagnostic masks, bboxes, pair means, or normalized rasters to authority;
- using the pair-mean top factor;
- local/component/row/feature warping;
- perspective reconstruction or camera solving;
- filling contours, closing contacts, inventing hidden surfaces, or deriving geometry;
- choosing a physical center, origin, pivot, centerline, snap anchor, topology, or component transform;
- revising or approving Step 10 items;
- beginning Step 11;
- Blender, FBX, texture, material, Unreal, collision, LOD, or performance work;
- modifying generic Core, pipeline, blueprint, or plan documents;
- staging, committing, or pushing before a separate scoped closeout approval.

## Exact Allowed Future Write Paths

Execution may create only these tracked records:

- `manifests/SCALE_AUTHORITY_RECOVERY_A01_INPUT_LOCK.json`;
- `manifests/SCALE_AUTHORITY_RECOVERY_A01_PHYSICAL_BOUNDS.json`;
- `manifests/SCALE_AUTHORITY_RECOVERY_A01_TRANSFORM_MANIFEST.json`;
- `manifests/SCALE_AUTHORITY_RECOVERY_A01_DISTORTION_HOLDOUT.json`;
- `manifests/SCALE_AUTHORITY_RECOVERY_A01_WORLD_INTEGRATION_OVERRIDE.json`;
- `manifests/SCALE_AUTHORITY_RECOVERY_A01_VALIDATION.json`;
- `steps/SCALE_AUTHORITY_RECOVERY_A01_OUTPUT_RECORD.md`;
- `handoffs/SCALE_AUTHORITY_RECOVERY_A01_TO_STEP10_REVISION_HANDOFF.md`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_FRONT_DERIVATIVE_REVIEW.png`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_BACK_DERIVATIVE_REVIEW.png`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_LEFT_DERIVATIVE_REVIEW.png`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_RIGHT_DERIVATIVE_REVIEW.png`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_TOP_DERIVATIVE_REVIEW.png`;
- `evidence/SCALE_AUTHORITY_RECOVERY_A01/SM_GIA_BloodAxeCairnstone_A005_SCALE_AUTHORITY_RECOVERY_A01_REVIEW_BOARD.png`.

Execution may use this local-only workspace:

- `Saved/AssetForgeResearch/A005_ScaleAuthorityRecovery_A01/`.

After Flamestrike's visible output decision, and only to record that decision, execution may update:

- `SM_GIA_BloodAxeCairnstone_A005_RESET_RESUME_STATE.md`;
- `SM_GIA_BloodAxeCairnstone_A005_ARTIFACT_INDEX.md`;
- `SM_GIA_BloodAxeCairnstone_A005_APPROVAL_LOG.md`;
- `docs/projects/assetforge/DRIFT_LEDGER.md` only if execution discovers a new, separately identified drift fact.

Local checkpoints may be created under `Saved/ProjectRecovery/`.

Any other write path is forbidden.

## Required Output Records

### Physical-Bounds Manifest

Must identify:

- approved choice ID;
- every target value, component role, and axis;
- which values are absolute production targets;
- which source labels support each target;
- statement that target approval does not retroactively validate historical per-view calibration;
- permitted and blocked consumers;
- Flamestrike approval record.

### Transform Manifest

Must include every field required by the Coordinate And Transform Rules and must preserve exact forward/inverse mapping between original and normalized coordinates.

### Distortion And Holdout Record

Must report, without hiding disagreement:

- front/back full-silhouette IoU and residuals after the approved global registration;
- left/right full-silhouette IoU and residuals after the approved global registration;
- top observed and transformed X/Y ratios;
- exact top X/Y factor used;
- per-axis scale change for every panel;
- maximum and median contour displacement;
- all target/source conflicts;
- confirmation that no holdout contour was used as a local fitting input;
- confirmation that no production pass threshold was invented.

### World/Integration Override

Must classify every affected scale/world conclusion as exactly one of:

- `recovered_authority_candidate`;
- `valid_raw_evidence_only`;
- `valid_physical_target_only`;
- `reference_only_after_recovery`;
- `blocked_source_target_conflict`;
- `reserved_for_step10_revision`.

It must never rewrite the affected historical field.

## Validators

Validation must prove:

- every input used was allowed, present, and hash-locked before output writes;
- all original source and Step 03 panel hashes remain unchanged;
- no existing Step 01-Step 10 record changed;
- the approved physical-bounds choice is recorded exactly;
- physical targets are separated from source-pixel calibration premises;
- front and left are the only primary upright shape owners;
- back and right are validation views;
- top X/Y factor is exactly `1.1260059213` with Y factor `1.0`;
- the pair-mean factor is absent from all production fields;
- every panel has exactly one complete global transform and inverse;
- forward/inverse coordinate round-trip error is numerically bounded and reported;
- derivative images were generated from original Step 03 crops using a declared nearest-neighbor review method;
- derivative images are labeled `proof only` and cannot be consumed as source pixels or textures;
- no local, component, row, feature, perspective, cage, lattice, spline, or content-aware warp exists;
- all holdout residuals and target/source conflicts are present;
- no physical center, pivot, snap anchor, hidden surface, interpretation, or geometry was created;
- only allowlisted paths changed;
- all output hashes and artifact statuses are recorded;
- the visible review board opens successfully;
- Step 10 revision, Step 11, and production remain stopped.

## Stop Conditions

Stop immediately and preserve the current authority if:

- the approved physical-bounds choice is missing or ambiguous;
- an input hash or status differs from its lock unexpectedly;
- source and target roles cannot be kept separate;
- a required transform needs local or component-specific warping;
- a global transform would clip source-owned pixels;
- a source/target conflict cannot be recorded without inventing geometry;
- an affected field cannot be traced through the reassessment index;
- any required output falls outside the allowlist;
- any existing Step 01-Step 10 record would need modification;
- the work begins resolving Step 10 interpretation instead of recovering scale authority.

On stop:

- do not repair forward;
- classify valid diagnostics as `proof only`;
- classify incomplete recovery records as `quarantined`;
- create a checkpoint and exact blocked handoff;
- report the missing authority to Flamestrike.

## Artifact Status On Technical Pass

Before Flamestrike's output decision:

- this approved execution contract: `authoritative for execution scope only`;
- input lock and validation: `proof only`;
- derivative panels and review board: `proof only`;
- physical-bounds, transform, distortion, world/integration, output, and handoff records: `candidate`;
- current A005 authority remains unchanged.

After visible Flamestrike approval, only explicitly approved bounded records may become `authoritative` for the recovered construction-frame routing. Step 10 still requires a separate revision contract.

## Artifact Status On Rejection Or Block

- source and prior approved authority remain unchanged;
- valid diagnostics: `proof only`;
- rejected transform/bounds package: `quarantined` or `invalid` as the evidence requires;
- Step 10, Step 11, and production remain stopped.

## Visible Review Requirement

Automatically open:

- this contract before execution approval;
- the review board after technical validation;
- the physical-bounds manifest;
- the transform summary;
- the distortion/holdout summary;
- the world/integration override;
- the validation manifest;
- the output record.

Clickable paths alone do not satisfy visible review.

## Smallest Sufficient Change

Create one bounded, recovery-suffixed scale/transform package and stop. Do not alter historical records, solve geometry, or revise Step 10 in the same pass.

## Planned Checkpoints For Future Execution

1. before opening any execution output path;
2. after the input lock and approved-bounds record;
3. after transforms and derivative panels, before world/integration classification;
4. after validation, before visible review;
5. after Flamestrike's output decision, before any separately approved closeout.

## Plain-English Execution Approval Question

Do you approve a recovery test that will:

- use `220 cm`, `120 cm`, `90 cm`, `140 cm`, `110 cm`, and `35 cm` as the final production size targets;
- use the front and left drawings as the main upright shape views;
- widen only the derivative top review image in X by `12.60059213%` relative to Y;
- use one global transform per whole panel and never warp individual parts;
- create review images and new recovery records;
- report every remaining mismatch;
- stop for your visual decision before changing Step 10 or making geometry?

Recommended exact approval statement:

`Approve A005-CR-SAR-A01 with A005-SAR-BOUNDS-A. Execute only the original-coordinate global transform, derivative review, distortion/holdout audit, and recovery-suffixed scale/world classification defined in the contract. Do not locally warp source evidence, revise Step 10, begin Step 11, create geometry, commit, or push.`
