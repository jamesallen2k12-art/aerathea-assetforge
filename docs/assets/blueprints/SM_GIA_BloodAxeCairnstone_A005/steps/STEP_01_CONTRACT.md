# Step 01 Contract - Fresh Project Charter And Asset Identity Lock

Status: completed and approved

Approved by: Flamestrike (`Proceed`, 2026-07-15)

## Decision

Approve or reject the proposed `SM_GIA_BloodAxeCairnstone_A005` identity,
isolated namespace, project goal, legacy-data firewall, completion definition,
and record layout.

## Core And Plan Authority

- Governing principles: First Principles, Evidence-Bound Decision Rule,
  closed-world authorization, and Stop-the-line.
- Authorizing plan: Sections 1-3, 6-9, 13 Step 01, and 16.

## Approved Inputs

- `AGENTS.md`
- authoritative fresh-start plan
- generic AetherForge and formal game-ready status/naming rules
- current git and recovery state
- A02 source and scanline paths as unopened reference strings only

## Allowed Files

Only the A005 tracked governance root, its Step 01 package, checkpoint-generated
local snapshots, the checkpoint journal append, and the approved minimal repair
to `Tools/System/aerathea_checkpoint.sh`.

## Blocked Files

All A001-A004 asset-specific data; the A02 source pixels and scanline contents;
all unrelated dirty files; all production roots.

## Allowed Actions

- checkpoint and A005 collision audit;
- create charter, firewall, reset, artifact index, approval log, Step 01
  contract/output/validation/handoff records;
- document source paths without opening source data;
- focused text/JSON/hash/scope validation;
- visible charter and firewall review;
- after explicit output approval, scoped checkpoint, commit, and push.

## Blocked Actions

Measurement, crops, masks, formulas, source verification, generated imagery,
asset scripts, geometry, DCC, UV, texture, FBX, Unreal, legacy access,
production-root creation, unrelated staging, and Step 02.

## Outputs

The nine A005 governance and Step 01 records named by the project layout.

## Validators

Path uniqueness; required-file and section completeness; JSON parse; output
hashes; firewall completeness; no A005 production path; scoped diff check; no
unrelated staging; checkpoint pointer; explicit Flamestrike output approval.

## Review

Open the charter and firewall in visible desktop windows. No image, geometry,
or rendered review is authorized.

## Status Rules

Before output approval, records are `candidate` and validation is `proof only`.
On approval, governance records become `authoritative`. On failure, preserve and
classify partial artifacts without repair-forward work.

## Stop Conditions

A005 collision; stale checkpoint pointer; authority conflict; legacy-data need;
out-of-scope write; inseparable staging; validator failure; output rejection;
or any request to begin Step 02.

## Smallest Sufficient Change

Create only the isolated identity and governance package. Produce no asset data.

## Context And Checkpoints

- Expected context: under 30k tokens, no production loop.
- Checkpoints: before write; after candidate validation before review; after
  output approval before scoped commit/push.

## Approved Recovery Amendment

The initial manual checkpoint exposed a stale `LATEST.md` pointer. Flamestrike
approved a narrow recovery pass. The checkpoint utility was minimally changed
so both manual and local-only checkpoints refresh `LATEST.md`, then Step 01
resumed. The repair does not authorize any other tooling change.

## Authorization Boundary

Contract approval authorized Step 01 only. Flamestrike approved the Step 01
outputs on 2026-07-15. Neither approval authorizes Step 02.
