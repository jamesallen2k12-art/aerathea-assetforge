# Twin Hammer Head, Contact, And Rotational-Station Authority A01 Review

- Date: `2026-07-24`
- Artifact status:
  `candidate partial authority resolution; production remains blocked`
- Measurement execution:
  `PASS`
- Independent audit:
  `PASS 261/261`
- Valid DCC source candidates: `0`
- Geometry or visual solution created: `no`

## Decision Summary

The handle measurement gap is substantially resolved as candidate authority.
The head construction gap is narrowed but remains genuinely blocked.

This review contains exact source edges, rational measurements, candidate
interpretation, and blocked unknowns only. It contains no fill, overlay,
surface preview, geometry, render, or generated image.

## Evidence

### Source-Lineage Evidence

- The new-R8 Step 06, Step 09, and Step 09A records are one internally linked
  measurement lineage.
- Step 09A owns component pixels, projected boundaries, protected negative
  spaces, and source order.
- Step 09A explicitly does not own hidden surfaces or point pairing.
- The original six concept images have different hashes and no approved exact
  coordinate mapping into the new-R8 dataset.
- The later correct-axis contract owns its explicit station semantics and
  physical locks.

### Exact Projected Contacts

| Contact | Shared source edge | Exact interval | Unit edges | 3D contact authority |
| --- | ---: | --- | ---: | --- |
| `C01/C06` | `y=600` | `[504,619)` | `115` | blocked |
| `C06/C07` | `y=670` | `[527,596)` | `69` | blocked beyond projection |
| `C12/C01` | `y=295` | `[487,636)` | `149` | blocked |

The C04 top/bottom spans were also expanded exactly, but they remain projected
owner boundaries rather than common contacts because their adjacent physical
stone owner is not explicit.

### Exact C06-C11 Stations

| Component | Source rows | Upper Z | Lower Z | Length |
| --- | --- | ---: | ---: | ---: |
| `C06_UPPER_HAFT_CAP` | `[600,670)` | `132` | `1022124/8591` | `111888/8591` |
| `C07_HAFT` | `[670,870)` | `1022124/8591` | `702444/8591` | `319680/8591` |
| `C07B_HAFT_TO_HANDLE_FERRULE` | `[870,955)` | `702444/8591` | `7980/121` | `135864/8591` |
| `C08_GRIP` | `[955,1110)` | `7980/121` | `2898/121` | `42` |
| `C09_LOWER_COLLAR` | `[1110,1150)` | `2898/121` | `18` | `720/121` |
| `C10_POMMEL_BODY` | `[1150,1220)` | `18` | `918/121` | `1260/121` |
| `C11_POMMEL_TERMINAL_CAP` | `[1220,1271)` | `918/121` | `0` | `918/121` |

The piecewise Z formulas are continuous at `955` and `1110`.

### Exact C08 Candidate Radius Owner

- Structural axis: source edge `x=562`
- C08 source rows: `[955,1110)`
- Rows replayed: `155`
- Lower registration domain: `[955,1271)`
- Maximum lower positive-half distance: `68 px`
- Exact radial scale: `11/136 cm/px`
- C08 positive-half distance range: `33..38 px`
- C08 radius range:
  `363/136..209/68 cm`
- C07 override remains:
  `5/2 cm`
- Every source column retains its exact rational `U` interval.

## Candidate Interpretation

The candidate source-role proposal is:

1. new-R8 sources own current pixels and measurements;
2. Step 09A owns projected component membership and boundary order;
3. the later correct-axis contract owns its explicit physical station/radius
   locks;
4. shared-depth bounds remain validation only; and
5. the original six-view lineage remains reference only unless a future exact
   mapping is separately approved.

The C08 radius table and the source-role split are candidate interpretation.
They do not become authoritative without Flamestrike's explicit decision.

## Blocked Head Authority

The six head boundary sequences contain:

- C02:
  `376 front / 446 top / 461 bottom`;
- C03:
  `376 front / 446 top / 461 bottom`;
- total samples:
  `2,566`.

Every front sample lacks authoritative hidden `Y`. Every top/bottom sample
lacks authoritative hidden `Z`, and axial world orientation/incidence is not
fully approved.

The common bounds cannot supply those coordinates.

No approved record supplies:

- a cyclic physical rail order;
- shared corner anchors;
- a fourth or hidden boundary rail;
- a complete XYZ embedding;
- an outward orientation; or
- a current hidden-closure equation.

Therefore the unequal-rail zipper prerequisites fail and exactly:

`0 triangle index triplets`

were emitted.

### Additional Head-Z Conflict

The new-R8 global front-view scale maps `A=600` to:

`114070/1063 cm`

The later physical lock sets:

`Z_A=132 cm`

No current approved equation maps the head rows while reconciling those two
registrations.

## Rotational Contact Findings

Only the `C08/C09` transition at source edge `1110` has an equal candidate
radius on both sides:

`209/68 cm`

The transitions at `670`, `870`, `955`, `1150`, and `1220` have unequal
radii. No current-lineage shoulder equation owns those differences. The
terminal boundary at `1271` also lacks a reconciled current closure equation.

## Independent Proof

- Candidate manifest SHA-256:
  `7a7c633587b4d39797339077685109eb28c31ca8e28e36f1e888d0506fec2ceb`
- Independent audit SHA-256:
  `837ebb6778867efa4f4228d30ac2f7d8e3c48f4303a3ea651b675f311995eaaa`
- Resolver SHA-256:
  `8d9abde1846b3d412af4dc0684b492e29f7aa7a59a83c7f324c88d6cd07d77cb`
- Auditor SHA-256:
  `75d3b54b4d6f5d476be1fec543aeaa32d3416a4e0ec2dd0ccb60da3d5b9bed2e`

Observed prohibited-output counts are all zero.

## Flamestrike Decision Gate

Choose:

`approve / revise / reject / blocked`

Approval means only:

- approve the candidate new-R8 source-role split;
- approve the projected-contact expansions;
- approve the C06-C11 station/Z table;
- approve the C08 radius-by-Z table;
- accept the named head, C04, transition, and terminal blocks; and
- permit a separately controlled authority lock.

Approval does not authorize a production builder, Blender, geometry, images,
rendering, export, Step 13, Step 14, or Unreal.
