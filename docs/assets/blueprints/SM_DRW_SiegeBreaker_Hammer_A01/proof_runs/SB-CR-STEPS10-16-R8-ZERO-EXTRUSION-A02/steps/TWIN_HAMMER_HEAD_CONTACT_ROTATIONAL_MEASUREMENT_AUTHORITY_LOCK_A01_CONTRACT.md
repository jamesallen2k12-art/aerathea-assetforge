# Twin Hammer Head, Contact, And Rotational Measurement Authority Lock A01 Contract

- Date: `2026-07-24`
- Run: `SB-CR-STEPS10-16-R8-ZERO-EXTRUSION-A02`
- Assets:
  - `SM_DRW_SiegeBreaker_Hammer_A01`
  - `SM_DRW_FoeHammer_Hammer_A01`
- Artifact status:
  `candidate contract pending exact-hash Flamestrike execution approval`
- Current authorized action:
  `draft and visibly review this one contract only`
- Measurement authority lock created by this draft: `false`
- Production authority: `false`
- Builder authority: `false`
- Blender authority: `false`
- Geometry authority: `false`
- Imagery / render authority: `false`
- Export authority: `false`
- Step 13 / Step 14 authority: `false / false`
- Unreal authority: `false`

## Current Draft Authorization

Codex asked:

> Do you approve drafting exactly one candidate measurement-authority lock
> contract for approved manifest SHA
> `7a7c633587b4d39797339077685109eb28c31ca8e28e36f1e888d0506fec2ceb`,
> with no production, Blender, geometry, imagery, render, export, or Unreal
> work?

Flamestrike replied:

> approved

That reply authorizes only creation and visible review of this candidate
contract. It does not authorize executing this contract, creating an
authority lock, changing any approved evidence, or advancing production.

## First-Principles Reduction

### Actual Project Goal

Protect the exact Flamestrike-approved measurement resolution against later
drift while preserving every unresolved head, C04, rotational-transition, and
terminal-cap block. The lock must make only the approved measurement subset
authoritative. It must not imply that a producible head, complete assembly, or
valid DCC source exists.

### Governing Blueprint Requirement

The authoritative shared-depth recovery blueprint requires:

- exact source ownership for every constructed surface;
- an explicit equation for every constructed connection;
- fail-closed handling through:
  `Blueprint block: source authority missing`;
- fail-closed handling through:
  `Blueprint block: rule missing`;
- no use of common bounds as unowned fill;
- no reuse of quarantined Step 12 geometry; and
- a separate approved production contract before builder, Blender, or
  geometry work.

The approved measurement result supplies only a partial measurement
resolution. It does not satisfy the missing head-surface requirements.

### Flamestrike-Approved Content

Flamestrike approved the exact candidate manifest after its Markdown review
was opened visibly. That approval covers only:

1. the candidate new-R8 source-role split;
2. exact projected-contact expansions;
3. the C06-C11 station and physical-Z table;
4. the C08 radius-by-Z table; and
5. acceptance of the named remaining blocks.

The approved manifest SHA-256 is:

`7a7c633587b4d39797339077685109eb28c31ca8e28e36f1e888d0506fec2ceb`

### Proven Evidence

- The measurement process result is
  `PASS_MEASUREMENT_PROCESS_WITH_PARTIAL_AUTHORITY_RESOLUTION`.
- The independent audit result is `PASS 261/261`.
- All `30/30` cited authority hashes replayed.
- All six new-R8 images decoded without resampling.
- Exact projected contact unit-edge count is `333`.
- The C06-C11 table contains seven half-open component intervals and eight
  station edges.
- The C08 table contains exactly `155` source-row records.
- Head boundary samples replayed: `2,566`.
- Complete head XYZ boundary samples: `0`.
- Head triangle index triplets: `0`.
- Geometry, image, render, Blender, export, and Unreal outputs: `0`.

### Interpretation

The following approved items remain interpretations until a later execution
creates and independently validates the authority lock defined here:

- the effective new-R8 versus original-lineage role split;
- the assembled C06-C11 station/Z table;
- the C08 radius-by-Z ownership rule.

Approval permits these exact interpretations to be locked. It does not
permit additional interpretation, smoothing, resampling, correspondence,
closure, or construction.

### Decision This Draft Must Produce

Flamestrike must decide whether the exact hash of this contract may be
executed to create one measurement-only partial authority lock and its
proof/review records. No execution follows automatically from this draft.

## Governing Input Package

Every future execution input below is immutable. Any hash mismatch is a
preflight block.

| Record | SHA-256 | Role |
|---|---|---|
| `AGENTS.md` | `5d2d2637a58113f0a6f5ec92e40d8bb2bda9e03fb58b4868a0d9dfbb3b57ad55` | Core and closed-world control |
| `manifests/SHARED_DEPTH_RECOVERY_BLUEPRINT_A01_AUTHORITY_LOCK.json` | `6889b826481e5e11dd10775f2b81467b1014687b7fda9ebbff62d519bfff09bc` | Existing shared-depth and production-boundary authority |
| `manifests/SHARED_DEPTH_RECOVERY_BLUEPRINT_A01_AUTHORITY_LOCK_VALIDATION.json` | `154ac1ed96c437fe971dfd27c415141f70ea08ae8ee43ae72251d1b30a016739` | Proof-only validation of the shared-depth lock |
| `steps/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_AND_ROTATIONAL_STATION_AUTHORITY_RESOLUTION_A01_CONTRACT.md` | `1352b2c008c30bda6e2755fc568126e62fc65e049b8a79c13808f4ea7e1a4548` | Consumed measurement execution contract |
| `manifests/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_ROTATIONAL_STATION_AUTHORITY_A01.json` | `7a7c633587b4d39797339077685109eb28c31ca8e28e36f1e888d0506fec2ceb` | Exact approved candidate measurement package |
| `manifests/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_ROTATIONAL_STATION_AUTHORITY_A01_INDEPENDENT_AUDIT.json` | `837ebb6778867efa4f4228d30ac2f7d8e3c48f4303a3ea651b675f311995eaaa` | Proof-only independent replay; `261/261 PASS` |
| `steps/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_AND_ROTATIONAL_STATION_AUTHORITY_RESOLUTION_A01_OUTPUT_RECORD.md` | `26941ec8eee3dc55494d8fe0dbc7d5079f6cad47642d2f3df8672356de122c5e` | Flamestrike output-decision record |
| `review/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_ROTATIONAL_STATION_AUTHORITY_A01_REVIEW.md` | `24d0e65baf95dc657f08947bffd7e41596dcd06cf3d3f1463d857b40ad859872` | Visibly reviewed measurement evidence |
| `handoffs/TWIN_HAMMER_HEAD_CORRESPONDENCE_CONTACT_ROTATIONAL_STATION_AUTHORITY_A01_HANDOFF.md` | `d6938adf4d4614bbf8ccb5b2a526b54937e1f8a60059092e32cc667f02a8f6be` | Approved scope and stop-state handoff |
| `manifests/STEP_STATE.json` at draft start | `a0785564b4e85a67c7bbe77da4b864ee13dd861a7925df8575601d55262a82cc` | Pre-draft state evidence only; expected to change only during a later approved lock execution |

The approved measurement package is recorded by repository commit:

`69846b0b66611d67e4821b8ad38c560a55a75351`

That commit reference is supporting provenance. The file hashes above remain
the controlling evidence.

## Immutable Manifest Rule

The approved candidate manifest must remain byte-identical.

A future lock must:

- reference the exact manifest path and whole-file SHA-256;
- promote only the approved subset by external authority-lock record;
- preserve the manifest's original internal candidate labels as historical
  pre-lock state;
- never rewrite, normalize, trim, reorder, or regenerate the manifest;
- never rerun the resolver to create a replacement manifest; and
- invalidate itself if the whole-file SHA-256 changes.

The independent audit must also remain byte-identical. It is proof only and
does not become an authority source.

## Canonical Partial-Lock Section Digests

The allowed subset is locked by immutable reference and canonical section
digest.

Canonicalization is exactly:

```text
jq -cS '<selector>' APPROVED_MANIFEST | sha256sum
```

The digest includes the single trailing newline emitted by `jq`. No other
serializer, whitespace convention, number conversion, or key rewrite may be
substituted.

| Approved subset | Exact selector | SHA-256 |
|---|---|---|
| Source-role split | `.source_role_and_lineage_matrix` | `3d1035aacddad1cae0340a847dbbc750847269b9ed6861650457af822ac29aae` |
| Projected contact and C04 boundary expansion | `.projected_contact_expansion` | `d7192621b27d67002124f09943a6265d90c84bb1191c156dbe5a3d40966d5b3c` |
| C06-C11 station and physical-Z authority | `.c06_c11_half_open_station_table` | `72cc13e2ab54fa132f4dc8675f198d17eb5262b2bd348def3d25147ba7a3a72d` |
| All 155 C08 radius-by-Z rows | `.c08_radius_by_z_table` | `eb82dba6e3d994533cca2150b4194d38ab2024fb8156a78c82bbd0f532c9ca6d` |
| Five governing stop conditions | `.remaining_blocks` | `f260d07eda22ef5faec9047d43a846b31f4849d16d08c1076cb68d1bf880bad4` |

## Exact Effective Authority After A Later Valid Lock Execution

If and only if this contract is separately approved by exact SHA-256 and the
future execution passes every gate, the authority lock may classify the
approved subset as:

`authoritative measurement-only partial authority; production remains blocked`

That effective status applies only as follows.

### Source-Role Authority

- The six new-R8 images own current view-local pixels, contours, component
  station observations, color observations, and view-local measurements.
- The original six-view lineage is reference only for current numeric work.
- Step 09A retains authority only for component membership, protected
  negative space, projected boundaries, and source order.
- Step 09A does not own hidden surfaces, point pairing, or triangulation.
- The shared-depth blueprint owns validation bounds and twin identity only.
  It creates no surface.
- The older component-equation contract supplies equation form and
  fail-closed precedent only. Its old image coordinates, scale, and stations
  remain excluded.

This source-role authority does not create an exact cross-lineage coordinate
mapping.

### Projected Contact Authority

The following are exact two-dimensional projected contacts only:

| Contact | Source edge | Half-open intersection | Unit edges |
|---|---:|---|---:|
| `FRONT_C01_C06_CONTACT` | `600` | `[504,619)` | `115` |
| `FRONT_C06_C07_CONTACT` | `670` | `[527,596)` | `69` |
| `FRONT_C12_RESERVED_C01_CONTACT` | `295` | `[487,636)` | `149` |

They do not create a 3D contact, point correspondence, common ring, surface,
or triangle.

The C04 top and bottom owner-boundary records are locked only as exact
projected owner boundaries. Their adjacent physical stone owner and complete
perimeter incidence remain blocked.

The bottom center-separation record remains an exact recorded absence of
bottom-specific samples. It is not fill or no-gap authority.

### C06-C11 Station And Physical-Z Authority

The exact half-open component intervals are:

| Component | Source rows |
|---|---|
| `C06_UPPER_HAFT_CAP` | `[600,670)` |
| `C07_HAFT` | `[670,870)` |
| `C07B_HAFT_TO_HANDLE_FERRULE` | `[870,955)` |
| `C08_GRIP` | `[955,1110)` |
| `C09_LOWER_COLLAR` | `[1110,1150)` |
| `C10_POMMEL_BODY` | `[1150,1220)` |
| `C11_POMMEL_TERMINAL_CAP` | `[1220,1271)` |

The exact piecewise physical-Z formulas are:

```text
600 <= y <= 955:
Z(y) = 132-(y-600)*(7992/42955)

955 <= y <= 1110:
Z(y) = 7980/121-(y-955)*(42/155)

1110 <= y <= 1271:
Z(y) = (1271-y)*(18/121)
```

The formulas must remain continuous at source edges `955` and `1110`.

This station authority applies to C06-C11 only. It does not reconcile the
head's global front-view registration at source edge `600`.

### C08 Radius-By-Z Authority

- Source rows: `[955,1110)`.
- Row count: `155`.
- Source axis edge: `x=562`.
- Exact radial scale: `11/136 cm/px`.
- Minimum observed distance: `33 px`.
- Maximum observed distance: `38 px`.
- Exact radius range:
  `363/136 cm` through `209/68 cm`.
- Every source column cell owns only its manifest-recorded angular interval.
- The exact 155 manifest rows are authoritative by immutable reference and
  section digest.

This authority does not permit smoothing, interpolation between unrecorded
rows, resampling, nearby-row substitution, or a fixed segment count.

### Governing Stop Conditions

The five manifest `remaining_blocks` records become authoritative stop
conditions. They are not solved by the lock.

## Explicit Non-Authority And Preserved Blocks

The following remain blocked after any valid execution of this contract.

### Blueprint block: source authority missing

- complete head XYZ embedding;
- physical head corner incidence;
- hidden-boundary owner;
- bottom center-gap disposition; and
- exact C04 adjacent stone owner and full physical perimeter incidence.

### Blueprint block: rule missing

- cyclic head boundary incidence;
- current hidden-closure equation;
- unequal-radius rotational shoulder/contact equations; and
- current-lineage terminal-cap closure.

### Blueprint block: source authority conflict

The new-R8 global front-view registration maps source edge `A=600` to:

`114070/1063 cm`

The later physical station authority places the same semantic station at:

`132 cm`

No current approved head-Z rule reconciles those values. Neither value may
silently overwrite the other for head construction.

### Head Correspondence And Closure

- All `2,566` replayed C02/C03 boundary samples remain incomplete in XYZ.
- Complete head XYZ samples remain `0`.
- Head point correspondences remain unapproved.
- Head triangle index triplets remain exactly `0`.
- The projected boundary inventory remains evidence only except where the
  approved projected-contact subset explicitly references it.
- The head-corner embedding matrix, head-closure incidence table, candidate
  correspondence table, and hidden-closure table do not receive production
  authority.

### Rotational Transitions

The complete `rotational_transition_contact_table` remains proof-only
candidate evidence. The explicit approved-scope list did not separately
promote that table. This includes the coordinate-equal C08/C09 observation at
source edge `1110`.

Every unequal-radius transition remains blocked without an approved shoulder
or contact equation. The source edge `1271` terminal-cap closure also remains
blocked. No transition row receives authority from this lock.

## Future Execution Preflight

A later execution must stop before creating any lock output unless all of the
following pass:

1. Flamestrike approved the exact SHA-256 of this contract for execution.
2. `AGENTS.md` matches its cited hash.
3. Every governing input path exists and matches its cited hash.
4. The approved manifest result remains
   `PASS_MEASUREMENT_PROCESS_WITH_PARTIAL_AUTHORITY_RESOLUTION`.
5. The independent audit remains `PASS 261/261` with zero failed checks.
6. The output decision remains `approved`.
7. The approved manifest whole-file hash remains exact.
8. All five canonical partial-lock section digests match.
9. The manifest still records `0` head triangle index triplets.
10. The manifest prohibited-output scan remains `PASS` with every prohibited
    count equal to zero.
11. `STEP_STATE.json` still reports:
    - authority lock created: `false`;
    - valid DCC source candidates: `0`;
    - active geometry contract: `false`;
    - production authority: `false`;
    - Blender authority: `false`;
    - geometry authority: `false`; and
    - Unreal authority: `false`.
12. No later Flamestrike decision conflicts with this approved measurement
    scope.
13. Every future output path declared below is absent.
14. A manual recovery checkpoint is created immediately before execution.

Skipped, unreadable, missing, conflicting, or unevaluated gates are failure,
not warning.

## Exact Allowed Future Outputs

After separate exact-hash approval of this contract, a later execution may
create exactly one authority lock:

- `../manifests/TWIN_HAMMER_HEAD_CONTACT_ROTATIONAL_MEASUREMENT_AUTHORITY_LOCK_A01.json`

It may also create only these direct proof and review records:

- `../manifests/TWIN_HAMMER_HEAD_CONTACT_ROTATIONAL_MEASUREMENT_AUTHORITY_LOCK_A01_VALIDATION.json`
- `TWIN_HAMMER_HEAD_CONTACT_ROTATIONAL_MEASUREMENT_AUTHORITY_LOCK_A01_OUTPUT_RECORD.md`
- `../review/TWIN_HAMMER_HEAD_CONTACT_ROTATIONAL_MEASUREMENT_AUTHORITY_LOCK_A01_REVIEW.md`
- `../handoffs/TWIN_HAMMER_HEAD_CONTACT_ROTATIONAL_MEASUREMENT_AUTHORITY_LOCK_A01_HANDOFF.md`

Minimum `STEP_STATE.json`, recovery-journal, and checkpoint updates are
allowed only to record the observed lock execution result and stop state.

No project tool may be created or modified. The resolver and independent
auditor must remain byte-identical.

The approved candidate manifest, its independent audit, its review, output
record, and handoff must remain byte-identical.

## Required Authority-Lock Record

The future authority-lock JSON must contain:

1. schema, date, run, and both asset IDs;
2. artifact status:
   `authoritative measurement-only partial authority; production remains blocked`;
3. this contract's exact approved SHA-256;
4. every governing input path and hash;
5. the approved manifest path and whole-file hash;
6. the five exact section selectors and canonical digests;
7. the approved source-role rules;
8. the three projected contacts and explicit `creates_3d_contact=false`;
9. the C04 projected-boundary-only and bottom-gap boundaries;
10. all seven C06-C11 half-open intervals;
11. all eight station edges and all three exact physical-Z formulas;
12. the exact C08 scale, row domain, row count, radius range, and section
    digest;
13. the explicit proof-only status of the complete rotational-transition
    table;
14. all five authoritative stop conditions;
15. exact authority-boundary booleans;
16. an invalidation rule for any governing hash or section-digest change; and
17. a next gate requiring a separate block-resolution contract before any
    production contract can be execution-ready.

Required authority-boundary booleans:

```text
measurement_partial_authority = true
complete_head_authority = false
head_correspondence_authority = false
head_closure_authority = false
c04_physical_contact_authority = false
unequal_radius_transition_authority = false
terminal_cap_closure_authority = false
builder_change_authority = false
builder_run_authority = false
blender_authority = false
geometry_authority = false
imagery_authority = false
render_authority = false
export_authority = false
step_13_authority = false
step_14_authority = false
unreal_authority = false
production_authority = false
```

## Independent Validation Requirements

The proof-only validation must be independent of the authority-lock writer.
It must:

- use read-only JSON, SHA-256, exact integer, and exact rational replay;
- not import or execute the measurement resolver;
- not import or execute the existing independent auditor;
- not trust lock-authored pass booleans;
- not create a new project validation tool;
- verify every governing input hash;
- verify the manifest whole-file hash;
- independently recompute all five canonical section digests;
- verify the approved output decision from the hash-locked records;
- verify the independent audit's `261/261 PASS` result and zero failures;
- verify three projected contacts and `333` unit edges;
- verify seven component intervals and eight station edges;
- replay continuity at `955` and `1110`;
- verify exactly `155` C08 rows and exact scale `11/136 cm/px`;
- verify the exact C08 radius range;
- verify the complete rotational-transition table remains outside the lock
  authority;
- verify `2,566` head samples, `0` complete head XYZ samples, and `0`
  triangle index triplets;
- verify every preserved block;
- verify every authority-boundary boolean;
- prove no geometry-like or image output exists; and
- classify every skipped, missing, unreadable, or unevaluated check as
  failure.

The validation is `proof only`. It cannot expand the lock scope.

## Failure, Block, And Drift Rules

### Preflight Block

If any governing hash, section digest, decision record, state boundary, or
future-path absence check fails:

1. do not create the authority lock;
2. record the exact mismatch in only the allowed output/handoff/state
   records needed for the block;
3. preserve all approved inputs unchanged;
4. create no substitute or regenerated manifest; and
5. stop for Flamestrike.

### Validation Failure

If the lock record is written but independent validation fails:

1. classify the lock `invalid`;
2. do not treat any candidate field as promoted by that lock;
3. preserve the failed lock and validation as evidence;
4. record the failure;
5. do not repair forward; and
6. stop for Flamestrike.

### Drift

If execution creates geometry, imagery, a solution overlay, a candidate
surface, an inferred coordinate, an uncited equation, or production output:

1. stop immediately;
2. identify the last Core-valid state and first drift action;
3. classify and quarantine the affected output;
4. record the event in `docs/projects/assetforge/DRIFT_LEDGER.md` and both
   affected asset status records; and
5. return to the last Core-valid state or present a Core reassessment.

## Explicitly Forbidden

- modifying the approved candidate manifest;
- modifying or rerunning the resolver or independent auditor;
- promoting the entire candidate manifest;
- promoting the projected boundary inventory as 3D authority;
- promoting any head correspondence, incidence, closure, or triangle;
- converting a projected contact into a 3D contact;
- treating C04 projected boundaries as physical stone contact;
- resolving the bottom center gap by inference;
- choosing between `114070/1063 cm` and `132 cm` for head construction;
- locking any rotational-transition row, including the coordinate-equal
  C08/C09 observation at source edge `1110`;
- locking any unequal-radius transition as a constructed shoulder;
- locking the terminal-cap endpoint as a closure equation;
- creating or changing production code;
- opening Blender;
- creating, modifying, copying, renaming, relinking, or saving geometry;
- images, image generation, renders, overlays, inferred fills, or previews;
- UV, texture, material, LOD, collision, FBX, GLB, or export work;
- Step 13 or Step 14 advancement;
- Unreal work;
- automatic revival of a consumed or blocked production contract;
- automatic production-contract drafting;
- automatic authority expansion; and
- self-approval of this contract.

## Review Visibility And Stop

This contract itself is the current review artifact.

It must be opened automatically in a visible desktop text-editor window.
A terminal path or clickable link alone does not satisfy review visibility.

After visible review, stop for one explicit decision:

`approve / revise / reject / blocked`

Approval must identify this contract by exact SHA-256. Only that exact
approval may authorize the future measurement-authority lock execution.

## Future Checkpoint And Repository Rule

A later approved execution must:

1. create a manual checkpoint before lock creation;
2. create a manual checkpoint after pass, block, failure, or drift;
3. stage only the exact dependency-complete approved scope;
4. commit and push only valid scoped tracked records to `assetforge/main`;
5. preserve every unrelated dirty worktree entry; and
6. stop after the lock review is visibly open.

This candidate contract does not authorize that future execution.

## Exact Next Gate

No authority lock or production action is active.

The next possible action is separate Flamestrike approval of this contract's
exact SHA-256 for execution. Even after a successful lock, production remains
blocked until the missing head, C04, unequal-radius transition, terminal-cap,
and head-Z authorities are resolved through separately approved contracts.
