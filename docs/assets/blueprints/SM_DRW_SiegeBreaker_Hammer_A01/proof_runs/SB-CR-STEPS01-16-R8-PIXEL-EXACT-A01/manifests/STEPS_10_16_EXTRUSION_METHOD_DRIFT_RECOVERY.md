# R8 Pixel-Exact Steps 10-16 Extrusion-Method Drift Recovery

- Date: `2026-07-23`
- Asset: `SM_DRW_SiegeBreaker_Hammer_A01`
- Run: `SB-CR-STEPS01-16-R8-PIXEL-EXACT-A01`
- Detected by: Flamestrike
- Flamestrike finding:
  `the presented result is an extrusion; yesterday's process has zero extrusions`
- Recovery state: `Core Recovery after Step 09`
- DCC / export / Unreal authority: `false / false / false`

## Last Core-Valid State

Steps 01-09 of this run remain the last valid state. They preserve:

- six hash-locked new orthographic sources;
- exact full-image and selected-object scanline/color replay;
- `1,544,766` selected source pixels;
- exact per-view uniform scales with `scale_x=scale_y`;
- the `170 cm` external height anchor;
- front-derived width `97.873941674506 cm`;
- right diamond-center rotation edge `x=557`;
- rune-side completed-depth consequence `34.434306569343 cm`;
- metal-center-piece completed-depth consequence `43.120437956204 cm`;
- exact component observations and cross-view disagreement records;
- exact cylinder-wrap authority
  `theta(U)=-pi/2+pi*U`,
  `X=r(z)cos(theta)`,
  `Y=r(z)sin(theta)`, and factor `pi/2`.

The Step 09 result remains `pass_with_explicit_blocks`. It authorizes no
geometry by itself.

## First Drift Action

Step 10/11 converted unresolved evidence into a new generalized cross-section
blueprint without binding the approved component-equation construction method.
Step 12 then built rectangular head slices and a simplified wrapped handle.
This was a new method, not a data substitution into yesterday's method.

## Why The Checks Did Not Protect The Result

The Step 12-16 checks proved file readability, bounds, topology, hashes,
exports, and internal reproducibility of the newly invented method. They did
not test the controlling requirement that the mesh be produced by the approved
zero-extrusion component reconstruction. A technically consistent wrong method
therefore passed its own checks.

## Affected Outputs And Classification

- `STEP_10_INTERPRETATION_DECISIONS.json`: `invalid`.
- `STEP_11_PRODUCTION_GEOMETRY_BLUEPRINT.json`: `invalid`.
- all Step 10-16 contracts, output records, handoffs, validations, audits,
  UV/material plans, meshes, LODs, collision, exports, renders, and final board:
  `invalid / quarantined`.
- `STEP_16_R8_PIXEL_EXACT_TWO_HAMMER_FINAL_REVIEW.png`: `invalid`.
- `Tools/DCC/build_siegebreaker_r8_pixel_exact_steps10_11.py`:
  `quarantined; forbidden as production input`.
- `Tools/DCC/build_siegebreaker_r8_pixel_exact_steps12_16.py`:
  `quarantined; forbidden as production input`.
- `Tools/DCC/audit_siegebreaker_r8_pixel_exact_steps12_16.py`:
  `reference only for explaining why the wrong method passed`.
- Steps 01-09 evidence and independent audits: `authoritative within their
  recorded exact-measurement scope`.

All affected files are preserved in place for defect history. None may be
imported, copied, scaled, repaired, or used as construction authority.

## Smallest Sufficient Recovery

Return to Step 09. Do not repair Step 12 geometry. Bind the exact approved
component-equation and A09 combined-boundary process to the new Step 01-09
measurements, add an independent zero-extrusion method audit, and only then
resume Step 10.

The exact reset instructions are:

`docs/assets/blueprints/SM_DRW_SiegeBreaker_Hammer_A01/handoffs/A12_R10_R8_ZERO_EXTRUSION_RESET_HANDOFF.md`
